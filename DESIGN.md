# DevOffice — 큰 그림 (Big Picture)

> 비개발자가 말로 제품을 만드는 Claude Code 플러그인.
> Phase 1: 플러그인 / Phase 2: GUI

---

## 0. 한 문장 정의

**DevOffice는 "코드를 대신 써주는 도구"가 아니라, "비개발자에게 개발 조직 하나를 붙여주는 도구"다.**

이 구분이 설계 전체를 결정한다. 코드 생성기라면 프롬프트만 잘 쓰면 된다.
조직이라면 **역할·문서·승인 절차·인수인계 규약**이 필요하다.
우리가 실제로 만드는 산출물은 에이전트가 아니라 **에이전트들이 따르는 업무 프로토콜**이다.

> 📄 **[DIALOGUE.md](./DIALOGUE.md)** — *사용자의 모호한 말을 어떻게 명세로 바꾸는가*.
> 가정 명세·질문 규격·확인 강도·오해 복구. **가장 상류에 있는 층이다.**
>
> 📄 **[PROTOCOL.md](./PROTOCOL.md)** — *조직이 어떻게 동작하는가*.
> 인수인계·진행 기록·동적 구성·검증·에스컬레이션·타당성 검토.
>
> 📄 **[DEPLOY.md](./DEPLOY.md)** — *어디에 올리는가*.
> 배포 사다리·git을 가르치지 않는 법·원격 서버 운영·실패했을 때의 탈출구.
>
> 📄 **[JOURNEY.md](./JOURNEY.md)** — *시작 이전과 완성 이후*.
> 진입 허들·운영 단계(S9~S12)·완주 설계·비용 가시화. **여정의 앞뒤를 채운다.**
>
> 📄 **[SAFETY.md](./SAFETY.md)** — *사용자가 묻지 않는 것들*.
> 보안·개인정보·백업·**회귀**·요금 사고. 요구되지 않으므로 어느 장치에도 걸리지 않는 층.
>
> 📄 **[CHANGE.md](./CHANGE.md)** — *이미 정했거나 만든 것을 바꿀 때*.
> 상태별 대응·영향 범위·데이터가 있을 때. **만드는 것보다 바꾸는 일이 더 많다.**
>
> 📄 **[CHECKPOINTS.md](./CHECKPOINTS.md)** — *사용자 차례 11번을 걸어가며*.
> 어디서 당황하고 무엇을 놓치는가. **준비물**과 **"이렇게 보이면 정상"** 규격.

---

## 1. 플랫폼 제약 — 설계를 강제하는 사실들

Claude Code 문서에서 확인한, 반드시 알고 설계해야 하는 제약이다.

| # | 사실 | 설계에 미치는 영향 |
|---|---|---|
| C1 | **서브에이전트는 `AskUserQuestion`을 쓸 수 없다** (모든 서브에이전트에서 제거됨) | 사용자와의 모든 대화·질문·승인은 **메인 세션**에서만 가능. "PM 에이전트가 사용자를 인터뷰한다"는 구조는 **불가능**하다 |
| C2 | 서브에이전트는 자기 시스템 프롬프트 + 지시문 + CWD만 받는다. 메인 대화 이력은 **전달되지 않는다** | 에이전트 간 공유 지식은 전부 **파일**로 오가야 한다 |
| C3 | 서브에이전트는 서브에이전트를 띄울 수 있다 (기본 3단계, `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH`로 조절) | CTO가 개발자를 부리는 구조가 기술적으로는 가능. 단 중간 과정이 사용자에게 안 보임 → 채택 여부는 §4에서 판단 |
| C4 | 플러그인 에이전트는 `hooks`, `mcpServers`, `permissionMode`를 지정할 수 없다 | 에이전트별 권한 격리는 `tools` / `disallowedTools`로만. 강제 규칙은 **플러그인 훅**으로 |
| C5 | 에이전트 `memory: project` → `.claude/agent-memory/<name>/`에 세션을 넘어 누적 | CTO에게 장기 기억을 줄 수 있다 (아키텍처 지식 축적) |
| C6 | 에이전트 `skills:` 프론트매터로 스킬 본문을 컨텍스트에 미리 주입 가능 | "우리 회사 규칙집"을 모든 에이전트에 동일하게 주입하는 수단 |
| C7 | 백그라운드 서브에이전트(기본값)는 도구가 축소된다 (Read/Grep/Glob/Bash/Edit/Write/Skill 등만) | 구현 에이전트에는 충분. 단 도구 목록 설계 시 유의 |
| C8 | 플러그인은 `bin/`에 실행 파일을 실어 Bash PATH에 올릴 수 있다 | 상태 관리·환경 점검을 **LLM이 아니라 스크립트**로 결정론적 처리 |
| C9 | 훅 이벤트에 `SessionStart`, `SubagentStart/Stop`, `PostToolUseFailure`, `FileChanged` 등이 있다 | 세션 복구·이벤트 로그·에러 번역의 기반 |
| C10 | Agent Teams는 실험적이고 환경변수로 꺼져 있으며 토큰 비용이 크다 | **Phase 1에서 쓰지 않는다.** 비개발자 대상 제품의 기반으로 부적합 |

> **C1이 이 프로젝트에서 가장 중요한 사실이다.** 대부분의 "AI 개발팀" 설계가 여기서 무너진다.

---

## 2. 설계 원칙 8가지

각 원칙은 위 제약이나 대상 사용자(비개발자)에서 직접 유도된다.

### P1. 창구는 하나, 작업장은 뒤에 있다
메인 세션 = **프로듀서(창구)**. 사용자와 대화하고, 질문하고, 승인을 받고, 순서를 통제한다.
서브에이전트 = **백오피스**. 사용자를 절대 만나지 않는다. 지시서를 받고, 파일을 남기고, 요약을 반환한다.
→ *(C1에서 강제됨)*

### P2. 파일이 조직의 기억이다
에이전트는 상태가 없다(stateless). 대화는 휘발된다. **디스크만 남는다.**
따라서 이 플러그인의 핵심 자산은 프롬프트가 아니라 `.devoffice/` 디렉토리 스키마다.
→ *(C2)*

### P3. 계약은 설득이 아니라 컴파일된다
"서버/클라이언트 에이전트가 API 규약을 잘 지키게 한다"는 문제의 정답은 **문서가 아니라 타입**이다.

```
packages/contracts/   ← CTO만 쓴다
  feature-001.ts      ← zod 스키마 + 요청/응답 타입 + 엔드포인트 상수
```
백엔드와 프론트엔드는 **반드시 이 파일을 import**해서 구현한다.
규약 위반은 에이전트의 리뷰 의견이 아니라 `tsc --noEmit` 실패로 잡힌다.
→ 산문 규약은 표류하지만, 컴파일되는 규약은 표류할 수 없다.

### P4. 사용자는 코드가 아니라 화면을 승인한다
비개발자는 diff를 읽을 수 없다. "완료"의 정의는 테스트 통과가 아니라
**"사용자가 직접 눌러보고 됐다고 말한 것"**이다.
→ 모든 기능은 `verify.md`(클릭 시나리오)와 실행 중인 앱으로 끝난다.

### P4-b. 한 번에 하나씩은 배려가 아니라 품질 장치다
한 번에 많이 만들면 **사용자와 에이전트가 동시에 누락한다.**
사용자는 검증할 게 많으면 대충 훑고, 에이전트는 작업이 크면 중간을 빠뜨리고도 완료라고 한다.

- **기능 하나씩 + 기능 자체에 크기 상한** (AC 5개 / 작업 8개 / 30분)
- **미검증 기능이 있으면 다음 작업이 기계적으로 차단된다.** 예외 없음
- **"있으면 좋은 것"은 만들지 않는다.** 백로그에 제안만
- 명세에 **"이번에 안 만드는 것"**을 적는다 — 이쪽이 더 중요하다

→ [PROTOCOL §11](./PROTOCOL.md)

### P5. 결정은 우리가, 승인은 사용자가
비개발자에게 Next.js vs Remix를 묻지 않는다. 스택은 **잠긴 기본값**이다.
사용자에게 던지는 질문은 전부 **제품 질문**이며, 가능하면 **객관식**이다(`AskUserQuestion`).
서술형 질문은 비개발자를 막히게 한다.

### P5-b. 추론은 없앨 수 없다. 목록으로 꺼낸다
비개발자는 모호하게 말하고, 모호한 입력을 정밀한 명세로 바꾸는 것이 이 제품이 파는 것이다.
**사용자를 훈련시키지 않는다.** 대신 무엇을 확인했고 무엇을 가정했는지를 **이유와 함께**
꺼내 보여준다. 추론 자체가 아니라 **보이지 않는 추론**이 문제다. → [DIALOGUE.md](./DIALOGUE.md)

### P6. 날것의 에러는 사용자에게 도달하지 않는다
비개발자 이탈의 1순위는 빨간 스택트레이스다.
`PostToolUseFailure` 훅 + 에러 번역 사전으로, 모든 실패는
**"무슨 일이 생겼는지 / 왜 / 지금 뭘 하면 되는지"** 세 줄로 변환된다.

### P7. 모든 것은 되돌릴 수 있다
기능 하나 = 브랜치 하나 = 커밋 하나 = 태그 하나.
사용자는 git을 모른 채 `/devoffice:undo`만 안다. **되돌릴 수 있다는 확신이 실험을 가능하게 한다.**

### P8. Phase 1의 파일 포맷이 Phase 2의 API다
GUI를 나중에 설계하면 늦다. 지금 `state.json` / `journal.jsonl` 스키마를 확정하면
Phase 2의 GUI는 **파일 감시자 + 렌더러**가 된다. 플러그인은 GUI의 존재를 몰라야 한다.

---

## 3. 조직도

```
                      ┌──────────────────────────┐
   사용자  ←──대화──→ │  프로듀서 (메인 세션)     │   ← 유일하게 사용자를 만남
                      │  질문·승인·순서·상태 전이 │
                      └────────────┬─────────────┘
                                   │ 지시서(파일) 전달
        ┌──────────────┬───────────┼───────────┬──────────────┐
        ▼              ▼           ▼           ▼              ▼
   ┌─────────┐  ┌───────────┐ ┌────────┐ ┌────────┐   ┌───────────┐
   │   CTO   │  │spec-writer│ │backend │ │frontend│   │    qa     │
   │ 기술총괄│  │  명세작성 │ │ 서버   │ │  UI    │   │ 검증(읽기)│
   └────┬────┘  └───────────┘ └───┬────┘ └───┬────┘   └───────────┘
        │                          │         │
        │  packages/contracts/ ────┴─────────┘   ← 두 구현체가 같은 타입을 import
        │  (CTO 전용 쓰기 권한)
        ▼
   ┌──────────┐   ┌────────────┐
   │  fixer   │   │ env-doctor │
   │ 에러수정 │   │ 환경/설치  │
   └──────────┘   └────────────┘
```

| 역할 | 활성 | 모델 | 권한 | 책임 |
|---|---|---|---|---|
| **프로듀서** (메인) | 상시 | Opus | 전체 | 사용자 대화, 승인 게이트, 단계 전이, 에이전트 호출 순서, **블로커 번역** |
| **cto** | 상시 | Opus, `memory: project` | 읽기 + `contracts/`·`.devoffice/` 쓰기 | 스택 잠금, **계약 작성**, 기능 분해, 재사용 조사, 타당성 3색 분류, **L3 증거 대조**, 그레이 영역 처리 |
| **research** | 상시 | Sonnet | 웹 + `.devoffice/research/` 쓰기 | **구현 가능성 실조사.** 실제 호출·요금·약관까지 확인하고 증거를 남김 |
| **backend** | 조건부 | Sonnet | 서버 경로만 쓰기 | API·DB·비즈니스 로직. 계약을 import해서 구현 |
| **frontend** | 조건부(1~2회) | Sonnet | UI 경로만 쓰기 | 화면·상호작용. `both`면 web·mobile 각각 호출 |
| **infra** | 조건부 | Sonnet | `.devoffice/infra/` 쓰기 | `server-vps`에서만. **절차서를 생성**한다(직접 실행 아님) + 요금 안전장치 |
| **qa** | 상시 | Sonnet, `disallowedTools: Edit, Write` | **읽기 전용** | 실행·검증 시나리오·스크린샷. 고칠 수 없기에 보고가 정직하다 |
| **fixer** | 상시 | Sonnet | 전체 쓰기 | L3에서 ❌ 난 항목 수리. 구현과 디버깅은 다른 모드이므로 분리 |
| **env-doctor** | 상시 | Haiku | Bash + 읽기 | `devoffice doctor` 결과 해석, 설치·계정 안내 |
| ~~spec-writer~~ | **보류** | — | — | 프로듀서가 직접 작성. 컨텍스트 압박이 실제로 확인되면 분리 |

정의된 역할은 8개지만 **한 프로젝트에서 동시에 활성인 것은 보통 5~6개**다.
로스터는 `state.json`이 들고 있고 프로젝트 구성에 따라 동적으로 켜진다
(→ [PROTOCOL §4](./PROTOCOL.md)).

### 왜 CTO가 개발자를 직접 부리지 않는가

기술적으로는 가능하다(C3). 하지만 채택하지 않는다:

- CTO가 사용자에게 질문할 수 없다(C1) → 중간에 판단이 필요하면 막힌다
- 중첩 에이전트의 중간 과정은 사용자 화면에 안 보인다 → 몇 분간 정적. 비개발자에게 최악
- 실패 시 어디서 끊겼는지 복구가 어렵다
- Phase 2 GUI가 진행 상황을 표현하기 어려워진다

**대신 권한을 이렇게 나눈다:**

> **순서를 통제하는 것은 프로듀서, 기술을 통제하는 것은 CTO.**

CTO의 권위는 지휘 계통이 아니라 **산출물**에서 나온다. CTO만 `contracts/`와 `plan.md`를 쓸 수 있고,
구현 에이전트는 거기서 벗어날 수 없다. 이게 실제로 더 강한 통제다.

*(완전 자동 모드가 필요해지면 그때 중첩을 옵션으로 켠다.)*

---

## 4. 작업 흐름

### 4.1 전체 단계

```
S-1 진입      터미널 열기 → 설치 한 줄 → /devoffice 호출     ← 가장 높은 허들
S0 인테이크   무엇을 만들고 싶은가 (대화)
S1 PRD        제품 정의서 + 기대 관리 + 콘텐츠 준비 안내      ★ 사용자 승인
S2 설계       "어디서 쓰나" 3지선다 → 스택 잠금  ★ 사용자에게는 요약만
S3 환경       계정 생성 + 도구 설치 (결정론적)
S4 골격       Hello World 배포 + 에러수집·문의창구 기반설비   ★ 첫 성공 경험
S5 백로그     기능 목록과 순서 (1번은 가시성 기준)            ★ 사용자 승인
S6 기능 루프  ── 반복 ── + 매회 진척도 표시     ★ 기능마다 승인 2회
S7 운영       수정·추가·롤백
S8 출시       앱스토어 제출 (행정 절차 안내)     ← 개발 루프와 분리 (§7)
S9 알리기     도메인·링크 미리보기·검색·모바일 확인
S10 듣기      에러 수집·문의 창구·사용 현황
S11 고치기    신고 → 수정 루프
S12 지키기    월간 점검·의존성·만료 관리
```

S-1과 S9~S12는 → **[JOURNEY.md](./JOURNEY.md)**

### 4.2 S4를 "로컬 Hello World"가 아니라 "실제로 도달 가능한 Hello World"로

요청하신 "Hello World까지 먼저"는 정확히 옳은 직감이다. 한 걸음만 더 밀면 훨씬 강해진다.

- 배포 파이프라인은 **나중에 붙일수록 비싸진다**. 기능 0개일 때 뚫는 게 가장 싸다
- 비개발자에게 **"내 것이 진짜로 존재한다"는 순간**의 심리적 효과가 크다. 여기서 이탈이 급감한다
- 이후 모든 기능이 "만들었다"가 아니라 "배포됐다"로 끝난다 (walking skeleton)

**프리셋별 S4 완료 정의:**

| 프리셋 | Hello World의 정의 | 사용자가 손에 쥐는 것 |
|---|---|---|
| `web-next` | Vercel에 배포되어 공개 URL로 접속됨 | **내 사이트 주소** |
| `mobile-expo` | Expo Go에서 QR로 열려 **본인 폰**에서 실행됨 | **내 폰에 뜬 내 앱** |
| `both` | 위 둘 다 | 주소 + 폰 화면 |

네이티브에서 "폰에 내 앱이 떴다"는 웹의 "내 주소가 생겼다"와 정확히 같은 무게의 사건이며,
**스토어 없이 도달 가능**하다. 그래서 스토어를 S8로 미룰 수 있다.

### 4.3 기능 루프 (핵심)

```
  ⓪  타당성    cto 3색 분류 → 🟡🔴만 research 실조사    ← 백로그 확정 전에 이미 끝나 있음
  ①  명세      프로듀서     →  spec.md (AC-1..N 번호 부여)
      ─────────────────────────────────────────  ★ 사용자 승인 게이트
  ②  계약      cto          →  packages/contracts/F-00X.ts
                               plan.md (작업 분해 + 소유권 + 재사용 지시)
                               gate plan: AC×작업 매트릭스 검사
  ③  지시      cto          →  handoff/NN-cto→*.md (작업 지시서)
  ④  구현      backend  ∥  frontend      ← 계약이 잠긴 뒤에만 병렬
                               작업마다 devoffice task done
                               막히면 추측 금지 → 블로커 발행
  ⑤  통합      cto          →  gate security(L0) ← 실패 시 무조건 차단
                               gate l1(타입·빌드) · gate l2(계약 스모크)
                               verification.md — AC별 파일:라인 증거 대조
                               "내 담당 아님" 신고 전부 처리
                          ❌ 하나라도 있으면 → fixer → 재대조 (⑤ 반복)
  ⑥  검증      qa           →  앱 실행 + verify.md + 스크린샷
      ─────────────────────────────────────────  ★ 사용자 확인 게이트
  ⑦  회귀      qa           →  verify.md를 자동 테스트로 변환 + 전체 회귀 실행
                               "사용자가 한 번 확인한 것은 다시 깨지지 않는다"
  ⑧  출시      프로듀서     →  커밋·배포·journal 기록·진척도 표시·다음 기능 제안
```

④~⑤ 중 어느 지점에서든 에이전트가 막히면 **추측하지 않고 멈춘다.**
블로커가 프로듀서에게 올라오고, 프로듀서가 사용자 언어 객관식으로 번역해 되묻는다.
→ [PROTOCOL §7](./PROTOCOL.md)

**"한 번에 하나씩"은 ①~⑥ 단위로 지킨다.** 단, ③의 FE/BE 병렬은 안전하다 —
②에서 계약이 컴파일 가능한 형태로 잠겼고, 파일 소유권이 분리되어 있기 때문이다.
이게 "오케스트레이션을 잘한다"의 구체적 정의다.

**파일 소유권 규칙 (충돌 방지)**

| 경로 | 소유자 |
|---|---|
| `packages/contracts/**` | cto **전용** |
| `supabase/**`, `apps/web/app/api/**`, `server/**` | backend |
| `apps/web/**` (api 제외) | frontend |
| `apps/mobile/**` | frontend (별도 호출) |
| `.devoffice/**` | 프로듀서 / spec-writer / cto |

> **`both` 프리셋에서도 에이전트를 늘리지 않는다.** `frontend`를 두 번 호출한다 —
> 한 번은 `apps/web`, 한 번은 `apps/mobile`. 둘 다 같은 계약을 import하므로
> 두 클라이언트가 자동으로 정렬된다. 이게 §7에서 말한 "최대 수혜 지점"의 실제 동작 방식이다.
> 에이전트를 늘리는 대신 **같은 에이전트를 다른 작업 지시서로 부른다**는 원칙을 유지한다.

---

## 5. `.devoffice/` — 조직의 파일 캐비닛

이 스키마가 이 프로젝트의 실질적 핵심 자산이다.

```
.devoffice/
├── state.json            현재 단계, 진행 중 기능, 게이트 승인 상태   ← GUI 데이터 소스
├── journal.jsonl         append-only 이벤트 로그 (훅이 기록)         ← GUI 실시간 피드
├── product/
│   ├── prd.md            사용자 언어로 쓴 제품 정의서
│   ├── stack.md          잠긴 기술 결정 + 이유
│   ├── architecture.md   구조 설명
│   ├── glossary.md       도메인 용어 사전 ← 에이전트 간 명명 일관성의 열쇠
│   └── playbook-*.md     역할별 규칙집 ← S2에서 CTO가 생성 (동적 구성의 핵심)
├── backlog.md            F-001..N + 타당성 3색 표시
├── research/
│   └── R-003-*.md        타당성 실조사 결과 (증거 URL·신뢰도·유효기간)
├── infra/
│   └── setup-guide.md    server-vps일 때만. 클릭 단위 절차서 + 요금 안전장치
├── features/
│   └── F-001-login/
│       ├── spec.md       사용자 스토리 + AC-1..N (번호 필수)
│       ├── plan.md       작업 분해 + 소유권 + AC×작업 매트릭스
│       ├── handoff/      작업 지시서 / 완료 보고서 (순번 파일명)
│       ├── worklog/      에이전트별 진행 체크포인트 ← 세션 재개의 근거
│       ├── blockers/     막힌 지점 (구조화)
│       ├── verification.md  AC별 파일:라인 증거 대조표
│       ├── verify.md     사용자가 직접 클릭할 검증 시나리오
│       └── status.json   ⓪~⑦ 각 단계 상태
└── decisions/
    └── ADR-001.md        왜 이렇게 결정했는가
```

각 디렉토리의 상세 규격은 [PROTOCOL.md](./PROTOCOL.md)에 있다.

`glossary.md`는 사소해 보이지만 중요하다. 에이전트마다 같은 개념을 `post`/`article`/`글`로 다르게
부르기 시작하면 코드가 조용히 갈라진다. 용어 사전은 그걸 막는 가장 싼 장치다.

### state.json (초안)

```json
{
  "version": 1,
  "phase": "FEATURE_LOOP",
  "product": { "name": "...", "oneLiner": "..." },
  "stack": { "preset": "next-supabase-vercel", "lockedAt": "2026-08-10T..." },
  "deploy": { "url": "https://...", "lastDeployedAt": "..." },
  "currentFeature": {
    "id": "F-003", "title": "댓글 달기", "step": "IMPLEMENT",
    "gates": { "specApproved": true, "userVerified": false }
  },
  "backlog": [ { "id": "F-004", "title": "...", "status": "pending" } ]
}
```

---

## 6. 플러그인 구성 매핑

```
devoffice/
├── .claude-plugin/plugin.json     name, userConfig(언어·스택 프리셋·배포 대상)
├── skills/                        ← 방법론이 사는 곳
│   ├── devoffice/SKILL.md         진입점·디스패처 (`/devoffice`)
│   ├── intake/ prd/ architect/
│   ├── setup/ skeleton/
│   ├── backlog/ build-feature/ verify/ ship/
│   ├── undo/ explain/ status/
│   └── _shared/                   회사 규칙집 (에이전트에 `skills:`로 주입)
│       ├── house-rules.md         코딩 규약·명명·폴더 구조
│       ├── stack-preset-*.md      스택별 플레이북
│       └── error-dictionary.md    에러 → 쉬운 말 번역 사전
├── agents/                        cto, research, backend, frontend, infra,
│                                  qa, fixer, env-doctor
├── hooks/hooks.json
├── output-styles/                 ← 비개발자용 출력 규격 강제 (JOURNEY §2.6)
├── settings.json                  subagentStatusLine — 대기 중 "지금 뭐 하는 중"
├── install.sh                     진입 한 줄 (Node→CC→플러그인→폴더→안내)
├── bin/devoffice                  ← 결정론이 사는 곳 (단일 실행 파일 + 서브커맨드)
│     state show                   현재 상태
│     task start|done|block <ID>   worklog+state+journal 동시 갱신
│     handoff new <from> <to>      지시서 템플릿 생성
│     gate spec                    가정 명세 존재 · 보류 항목 백로그 편입 검사
│     gate plan|l1|l2              계획 매트릭스 / 타입·빌드 / 계약 스모크
│     doctor                       로컬 환경 점검 (멱등·재실행 가능)
│     remote doctor|bootstrap      원격 서버 점검 / 초기 세팅 1회 명령
│     remote deploy|rollback       rsync 배포 / 되돌리기
│     remote logs|status|snapshot  원격 진단 — 스냅샷은 파일로 남는다
│     remote destroy               정리 안내 (요금 중단)  → DEPLOY.md
└── templates/
    ├── scaffold/                  스캐폴딩 원본
    └── playbooks/                 프리셋별 역할 규칙집 원본 ← 기여 진입점
```

### 훅 설계 — "지시"가 아니라 "강제"

프롬프트로 쓴 규칙은 언젠가 어겨진다. 훅으로 쓴 규칙은 어겨지지 않는다.

| 이벤트 | 하는 일 | 왜 |
|---|---|---|
| ~~`SessionStart`~~ | **철회 (D26').** 플러그인이 먼저 말을 걸지 않는다. 이어하기는 스킬이 한다 | 사용자가 시작 시점을 통제해야 한다 |
| `PreToolUse(Bash)` | `.env` 스테이징된 커밋 차단, 위험한 삭제 차단 | 비밀키 유출 방지. 프롬프트로는 못 막는다 |
| `PostToolUseFailure` | 에러를 사전 기반으로 쉬운 말 + 다음 행동으로 변환 | P6 |
| `SubagentStart/Stop` | `journal.jsonl`에 이벤트 기록 | Phase 2 GUI의 연료 |
| `Stop` | 게이트 미승인 상태로 다음 단계 넘어가려 하면 경고 | "한 번에 하나씩" 강제 |

### 비밀키 취급 규칙

비개발자는 API 키를 채팅창에 붙여넣는다. 반드시:
1. 키는 항상 `.env.local`로 유도하고 **대화에 남기지 않는다**
2. `.gitignore`는 스캐폴딩 시점에 강제 생성
3. 커밋 훅으로 이중 차단

---

## 7. 스택 잠금

산출물 범위에 **네이티브 앱을 포함**하기로 확정했다(§12). 따라서 스택 프리셋은 2개가 되고,
S2에서 사용자에게 **딱 한 번** 3지선다를 묻는다. 이건 기술 질문이 아니라 제품 질문이므로 P5에 부합한다.

> "만드시는 걸 사람들이 **어디서** 쓰게 될까요?"
> ① 웹 브라우저에서 ② 스마트폰 앱으로 ③ 둘 다

### 프리셋 A — `web-next` (①)

| 층 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js (App Router) + TypeScript | FE/BE가 한 저장소. 사용자가 관리할 대상이 하나 |
| 스타일/컴포넌트 | Tailwind + shadcn/ui | **디자인 결정을 1회로 종결** |
| DB·인증 | Supabase (Postgres + Auth) | 비개발자가 직접 구현하면 안 되는 영역 |
| 배포 | Vercel | GitHub 연동 후 push = 배포 |

> **백엔드 형태는 별도로 결정된다.** 위 표의 Supabase는 기본값일 뿐이고,
> BaaS / 매니지드 서버 / VPS(EC2) 중 무엇이 필요한지는 **CTO가 PRD를 읽고 판정**한다.
> 사용자에게 "EC2를 쓸까요?"라고 묻지 않는다 → [PROTOCOL §4.4](./PROTOCOL.md)

### 프리셋 B — `mobile-expo` (②), `both` (③)

| 층 | 선택 | 이유 |
|---|---|---|
| 앱 | Expo (React Native) + TypeScript | **로컬 네이티브 툴체인 불필요**(아래 참조) |
| UI | NativeWind + Expo Router | 웹 프리셋과 Tailwind 문법을 공유 → 지식·규칙집 재사용 |
| 백엔드 | Supabase (Postgres + Auth + Storage) | 앱만 만들 때 서버를 따로 세우지 않아도 됨 |
| 배포(개발) | Expo Go / EAS Update | 스토어 없이 실기기에서 즉시 확인 |
| 배포(출시) | EAS Build + Submit | 클라우드 빌드 |
| 구조 | pnpm workspace 모노레포 | ③ 선택 시 `apps/web` + `apps/mobile` + `packages/contracts` |

> **③(둘 다)에서 `packages/contracts`가 진짜 값어치를 한다.**
> 웹과 앱이 물리적으로 분리된 클라이언트가 되므로, 컴파일되는 계약(P3)이 없으면
> 두 클라이언트가 조용히 갈라진다. 여기가 이 설계의 최대 수혜 지점이다.

### 네이티브를 비개발자에게 가능하게 만드는 핵심 결정

**로컬에 Xcode·Android Studio를 설치시키지 않는다.**

이게 네이티브 지원의 성패를 가른다. 비개발자에게 Xcode 설치(수십 GB, 애플 계정, 라이선스 동의)는
사실상 이탈 지점이다. 대신:

- **개발 중**: Expo Go 앱 설치 → QR 코드 → 본인 폰에서 즉시 실행. 로컬 툴체인 0
- **빌드**: EAS Build가 **클라우드에서** 빌드. 맥이 없어도 iOS 빌드가 나온다
- 결과적으로 S3(환경 설정)의 요구사항이 웹 프리셋과 거의 같아진다 — Node + 에디터 + 폰

### 앱스토어 배포에 대한 솔직한 선긋기

스토어 배포는 코드 문제가 아니라 **행정 문제**다. 개발자 계정(Apple $99/년, Google $25),
인증서·프로비저닝, 심사 대기, 스토어 정책 — 에이전트가 대신해줄 수 없는 영역이다.

따라서 **기능 개발 루프에서 분리**한다:

- S4~S6(개발·검증)은 전부 **Expo Go / EAS Update**로 완결된다. 스토어 없이 앱이 폰에서 돈다
- 스토어 제출은 별도 단계 **S8 `/devoffice:release`** — "코드 자동화"가 아니라 **"행정 절차 안내"** 스킬로 설계
- 사용자가 스토어 없이도 제품을 계속 만들 수 있게 하는 것이 목적

> **"디자이너 에이전트"를 두지 않는 이유:** 디자인 시스템(shadcn/ui 또는 NativeWind 테마)을
> S4에서 한 번 잠그면 이후 화면의 80%가 자동으로 일관된다. 매 기능마다 디자인 에이전트를
> 부르는 것보다 훨씬 싸고 결과가 일관적이다.

---

## 8. Phase 2 (GUI)를 위해 지금 해야 할 단 한 가지

**플러그인이 상태를 기계가 읽을 수 있게 쓰는 것.** 그게 전부다.

```
  Phase 1 (플러그인)                    Phase 2 (GUI)
 ┌──────────────────┐                ┌──────────────────────┐
 │ .devoffice/      │ ──파일 감시──→ │ 진행 상황 보드       │
 │   state.json     │                │ 기능 칸반            │
 │   journal.jsonl  │ ──이벤트────→  │ 에이전트 활동 피드   │
 │   features/*.md  │ ──렌더링───→   │ PRD/명세 편집기      │
 └──────────────────┘                └──────────┬───────────┘
          ▲                                     │
          └────── Agent SDK / claude -p ────────┘
                     (지시 주입)
```

**설계 규칙 3가지**

1. **Headless-first.** 플러그인은 GUI 없이 CLI만으로 완결되어야 한다. GUI는 뷰어 + 리모컨이다
2. **플러그인은 GUI를 모른다.** GUI가 플러그인에 의존하지, 그 반대가 되면 안 된다
3. **`state.json`에 `version` 필드를 처음부터 넣는다.** 스키마는 반드시 바뀐다

> 부수적 발견: 플러그인의 `monitors/` 컴포넌트로 로컬 브릿지 서버를 띄울 수 있다.
> Phase 2에서 "플러그인이 GUI를 자동으로 켜주는" 경로가 열려 있다.

---

## 9. 예상되는 실패 지점 (미리 알고 설계할 것)

| 위험 | 대응 |
|---|---|
| **비용·시간.** 기능 1개당 서브에이전트 5~6회 = 수 분 + 상당한 토큰 | Opus는 CTO에만. 구현은 Sonnet, 환경 점검은 Haiku. 결정론적 작업은 `bin/` 스크립트로 |
| **사용자가 인수 조건을 못 쓴다.** "로그인 되게 해줘"가 전부 | 프로듀서가 초안을 쓰고 사용자는 O/X만. 질문은 최대한 객관식 |
| **계정 생성은 자동화 불가.** GitHub·Vercel·Supabase는 사람이 브라우저에서 | 가이드 품질 = 이탈률. 스크린샷 수준의 단계별 안내가 필요. S3가 제일 공들일 구간 |
| **스코프 크립.** 검증 단계에서 "이것도 되게 해주세요"가 나온다 | 새 요구는 **자동으로 백로그에 편입**하고 현재 기능은 끝낸다. 규칙으로 못 박기 |
| **세션 단절.** 비개발자는 창을 닫는다 | `devoffice` 스킬이 `state.json`을 읽어 이어하기 (D26') |
| **에이전트 간 명명 표류** | `glossary.md` + 계약 파일 (§5, P3) |
| **오픈소스 확장성** | 스택을 `stack-preset-*.md`로 플러그인화 → 커뮤니티가 프리셋을 기여 |

---

## 10. Phase 1 구축 순서

우리도 우리가 제안하는 방식대로 만든다 — **walking skeleton 먼저.**

### v0.1 — 첫 출시 경계 (확정)

| 단계 | 산출물 | 완료 기준 |
|---|---|---|
| **0** | **S-1 진입** — `install.sh` 한 줄 + 터미널 스크린샷 + 설치 후 안내 (macOS) | **이게 없으면 아무도 도달하지 못한다** |
| **0.5** | **출력 규격** — `output-styles/` + 침묵 예고 + 오류 해석 | CLI에서 비개발자가 길을 잃지 않는다 |
| **1** | `.devoffice/` 스키마 + `bin/devoffice` (`state`·`doctor`) | 상태를 읽고 쓸 수 있다 |
| **2** | `/devoffice:start` → 인테이크 → PRD | 대화만으로 PRD가 나온다. **여기서 가치 가설이 검증된다** |
| **2.5** | `research` 에이전트 (🔴 수준만) | **제품의 핵심 전제가 성립하는지**를 PRD 단계에서 확인. 기능별 정밀 조사는 v0.2 |
| **3** | `skeleton` (프리셋 A·B) + 백엔드 형태 판정 | 비개발자가 **배포된 Hello World**를 갖는다 |

**v0.1은 기능을 하나도 만들지 못한다. 그래도 출시한다.**
"아이디어를 말했더니 PRD가 나오고, 내 사이트/내 앱이 생겼다"는 경험만으로 제품이 성립하며,
이후 기능 루프 설계를 **실사용자 피드백으로 교정**할 수 있다. 기능 루프는 이 설계에서
가장 비싸고 가장 틀리기 쉬운 부분이므로, 추측으로 먼저 완성하지 않는다.

v0.1에서 반드시 함께 나가야 하는 최소 안전장치:
`PreToolUse` 비밀키 차단 + `PostToolUseFailure` 에러 번역 + 스킬의 이어하기.
이 셋이 없으면 비개발자가 3단계를 통과하지 못한다.

### v0.2 이후

| 버전 | 산출물 |
|---|---|
| **v0.2** | 기능 루프 1회전 (PROTOCOL §2·3·5) + **L0 보안 게이트** + 진척도·기대 관리·복귀 요약 |
| **v0.3** | **L5 회귀** (기능 5개 넘기기 전 필수) + 에스컬레이션·그레이 영역 + S4 기반 설비 |
| **v0.4** | 나머지 훅, `journal.jsonl`, `undo`/`explain`/`status` + S9~S11 (알리기·듣기·고치기) |
| **v0.5** | 개인정보 게이트·백업·요금 방어 + S12 점검 + 비용 가시화 + 비개발 산출물 |
| **v1.0** | `infra` 에이전트 + VPS 절차서, S8 스토어 출시 안내, 프리셋 확장 |

> **L0(보안)과 L5(회귀)의 순서가 중요하다.** 보안은 기능마다 쌓이는 부채라 v0.2부터,
> 회귀는 기능이 몇 개 쌓여야 의미가 생기지만 **필요해졌을 때 만들면 이미 늦다** —
> 그때는 과거 기능의 검증 시나리오가 자동화되어 있지 않다. → [SAFETY §12](./SAFETY.md)

---

## 11. 언어 전략 (확정: 전면 한국어 우선)

| 대상 | 언어 | 비고 |
|---|---|---|
| 사용자 대면 출력 전부 | **한국어** | 질문·설명·에러 번역·가이드 |
| `.devoffice/` 산출물 (PRD·명세·검증 시나리오) | **한국어** | 사용자가 읽고 승인하는 문서 |
| 에이전트 시스템 프롬프트 / 스킬 본문 | **한국어** | |
| README·문서 | **한국어** (영어는 후순위) | |
| 코드·식별자·경로·파일명·커밋 메시지 | **영어** | 한글 경로는 도구 호환성 문제를 일으킨다 |
| `packages/contracts/` 내부 | **영어** (주석은 한국어 허용) | |

- `plugin.json`의 `userConfig`에 `language` 옵션을 처음부터 넣어두되, v0.1은 `ko` 고정
- **`glossary.md`가 이 전략의 접착제다.** 사용자는 "글", 코드는 `post` — 이 매핑을
  용어 사전이 보관하고 모든 에이전트가 참조한다. 한국어 우선일수록 이 파일이 더 중요해진다

---

## 12. 확정된 결정

| # | 결정 | 반영 위치 |
|---|---|---|
| D1 | 산출물 범위에 **네이티브 앱 포함**. 프리셋 2종 + `both` | §7, §4.2, §4.3 |
| D2 | 네이티브는 **로컬 툴체인 없이** Expo Go + EAS Build | §7 |
| D3 | 앱스토어 제출은 개발 루프에서 분리해 **S8**로 | §4.1, §7 |
| D4 | **v0.1 = 배포된 Hello World까지.** 기능 루프는 v0.2 | §10 |
| D5 | **전면 한국어 우선.** 단 코드·경로·식별자는 영어 | §11 |
| D6 | 인수인계는 **작업 지시서 / 완료 보고서** 쌍. 진행 기록은 **작업 단위 체크포인트**로 분리 | PROTOCOL §2·3 |
| D7 | 동적 구성은 **정적 역할 + 동적 플레이북**. 에이전트 파일을 런타임에 만들지 않는다 | PROTOCOL §4 |
| D8 | 백엔드 형태는 **CTO가 PRD로 판정**하고 제품 트레이드오프로 번역해 보고 | PROTOCOL §4.4 |
| D9 | 검증은 **4중 게이트 + 증거주의**. 근거를 못 대면 미구현으로 간주 | PROTOCOL §5 |
| D10 | 그레이 영역은 **"내 담당 아님" 신고**로 수집하고 CTO가 전량 처리 | PROTOCOL §6 |
| D11 | **막히면 멈춘다.** 추측 진행 금지, 구조화 블로커 발행 | PROTOCOL §7 |
| D12 | 타당성은 **3색 분류 + 🟡🔴만 실조사**. 증거 URL 없는 주장 금지 | PROTOCOL §8 |
| D13 | `research`·`infra` 에이전트 신설, `spec-writer` 보류 | §3, PROTOCOL §10 |
| D14 | 배포는 **5칸 사다리**. 기본은 1칸, 올라가는 건 필요할 때만. 칸 사이 이동 비용을 낮게 유지 | DEPLOY §2 |
| D15 | **GitHub과 배포를 분리.** GitHub은 금고(자동), 서버 전송은 rsync. git 사용법을 가르치지 않는다 | DEPLOY §3 |
| D16 | SSH 접속 이후는 **전부 자동** — `remote bootstrap` 1개 명령. 사람의 몫은 콘솔 7단계뿐 | DEPLOY §4·8 |
| D17 | 원격 진단은 **로컬 세션에서** `remote logs/status/snapshot`. EC2에 Claude 세션을 여는 건 기본 아님 | DEPLOY §5 |
| D18 | "AWS를 쓰고 싶다"는 요구는 **Lightsail**로 유도 (고정 요금 = 요금 폭탄 위험 제거) | DEPLOY §2 |
| D19 | **가정 명세**가 모든 명세의 고정 섹션. 확인된 것 / 정한 것(+이유) / 정해주셔야 할 것 3분할 | DIALOGUE §3 |
| D20 | 확인 강도는 **모호함 × 되돌리기 비용**으로 조절. 전부 확인하면 사용자가 지쳐 정확도가 떨어진다 | DIALOGUE §5 |
| D20-a | 모호함 판정은 감이 아니라 **해석 갈래 × 갈래 간 결과 격차 × 가정 개수** 3축 | DIALOGUE §5.1 |
| D20-b | 되묻기는 **5단계**. 강도 1·2는 **응답을 요구하지 않는다** — "묻지 말고 말해라" | DIALOGUE §5.2 |
| D20-c | **기능당 응답 요구 질문 2개 예산.** 초과분은 자동으로 강도 2로 강등 | DIALOGUE §5.4 |
| D20-d | 질문은 **모아서 한 번에.** CTO 미결 사항은 `plan.md`의 `## 확인 필요`로 집결 | DIALOGUE §5.5 |
| D20-e | 임계는 **적응형** — 오해 신호가 오면 자동으로 더 신중해진다 | DIALOGUE §5.6 |
| D21 | 질문 4요소 필수: **결과 · 추천 · "모르겠어요" · "나중에 바꿀 수 있어요"** | DIALOGUE §4.2 |
| D22 | 같은 기능 3회 수정 → 부분 수정 중단, **`SPEC_WRONG`**으로 명세 재작성 | DIALOGUE §7 |
| D23 | "알아서 해주세요"는 **통보 + 기록 + 되돌리기 가능** 모드로 처리 | DIALOGUE §8 |
| D24 | 불확실하면 **껍데기(UI만) 먼저** 보여주고 확인받는다 | DIALOGUE §9 |
| D25 | **Phase 1은 CLI로 완결된다.** 다른 표면으로 UX를 우회하지 않는다 — headless-first(§8) 위반 | JOURNEY §2.0 |
| D25-a | 진입은 **설치 스크립트 한 줄.** 사용자가 타이핑하는 것은 그 한 줄뿐 | JOURNEY §2.1 |
| D25-b | **출력 규격 6원칙**을 `output-styles/`로 강제. CLI 사용성은 v0.1의 기능 그 자체 | JOURNEY §2.6 |
| D25-c | 침묵은 **예고 + 상태줄 + 작업 분할** 3겹으로. "화면이 조용해도 정상이에요" | JOURNEY §2.7 |
| D25-d | 오류는 숨기지 않고 **아래에 해석을 붙인다.** "무시하셔도 돼요"가 핵심 | JOURNEY §2.8 |
| D25-e | 읽는 문서는 `devoffice open`으로 **브라우저에 뺀다** — Phase 2 GUI의 최초 형태 | JOURNEY §2.9 |
| ~~D26~~ | ~~`SessionStart`가 스스로 대화를 연다~~ → **철회.** 아래 D26' 참조 | — |
| **D26'** | **플러그인은 먼저 말을 걸지 않는다.** 사용자가 `devoffice` 스킬을 불러야 시작된다 | JOURNEY §2.3 |
| D27 | 단계를 **S-1 · S9~S12로 확장.** 사용자의 목표는 코드가 아니라 **운영**이다 | JOURNEY §3·7 |
| D28 | 매 기능 완료 시 **진척도 + "이 속도면 언제 끝나는지"**를 보여준다 | JOURNEY §4.1 |
| D29 | 백로그 1번은 **난이도가 아니라 가시성**으로 고른다 | JOURNEY §4.4 |
| D30 | **L0 보안 게이트** 신설 — 다른 게이트보다 먼저, 실패 시 무조건 차단. 질문하지 않고 고치고 통보 | SAFETY §3 |
| D31 | **L5 회귀 게이트** — `verify.md`가 그대로 자동 테스트가 된다. "사용자가 한 번 확인한 것은 다시 깨지지 않는다" | SAFETY §6 |
| D32 | 개인정보 처리방침은 **스키마에서 수집 항목을 추출해 자동 생성**. 필드 추가 시 자동 갱신 | SAFETY §4 |
| D33 | `state.json`에 **`hasRealData`** — 실 데이터가 생기면 파괴적 작업의 문턱이 올라간다 | SAFETY §5 |
| D34 | `--dangerously-skip-permissions`를 **권장하지 않는다.** 대신 제품 상자 안은 자유·밖은 차단 | SAFETY §9 |
| D35 | 모든 승인 요청에 **되돌리기 정보**를 붙인다. 없으면 사용자는 최악을 가정한다 | CHECKPOINTS §3.1 |
| D36 | 모든 응답이 **👉 ⏳ 🔗** 중 하나로 끝난다 — 지금이 누구 차례인지 | CHECKPOINTS §3.2 |
| D37 | PRD·백로그는 **문서가 아니라 요약 3가지**로 승인받는다. 문서는 볼 수 있게만 | CHECKPOINTS §4 |
| D38 | 웹/앱 선택지에 **이름이 아니라 결과**를 쓴다. 앱의 추가 부담은 **고르기 전에** 알린다 | CHECKPOINTS §4 |
| D39 | 계정은 **전체 목록·무료 여부를 먼저** 보여주고, `accounts.md` 기록장을 만든다 | CHECKPOINTS §4 |
| D40 | Hello World 대신 **제품 이름이 들어간 첫 화면** | CHECKPOINTS §4 |
| D41 | **검증 준비물**을 우리가 만든다 — 테스트 계정 2개·시드 데이터·직링크. 없으면 계획 게이트 실패 | CHECKPOINTS §5 |
| D42 | 모든 확인 항목은 **행동 + "~하면 정상이에요"** 두 부분. 기대 결과 없으면 게이트 차단 | CHECKPOINTS §6 |
| D43 | 설명이 어려우면 **캡처를 받는다.** 비개발자에게 증상 언어화를 요구하지 않는다 | CHECKPOINTS §6 |
| ~~D44~~ | ~~셸 런처~~ — **논외 처리** | — |
| D45 | 제품 이름은 **S0 끝에 후보 3개를 제시**해서 정한다. 빈칸을 주지 않는다. 영문 슬러그는 자동 생성 | CHECKPOINTS §4 (차례 1.5) |
| D45-a | 이름 변경 비용을 구분: **표시 이름은 쉽다 / 주소는 링크가 깨진다 / 폴더는 안 바꾼다** | CHECKPOINTS §4 |
| D46 | 무료 한도를 **제품 언어로 환산**해서만 말한다 (1GB → 사진 1,000장). 환산표를 플레이북 상수로 | SCENARIO §9.3 |
| D47 | 백로그 1번 = **"의존성 없으면서 가장 눈에 보이는 것".** 없으면 의존 기능을 가시적 조각으로 쪼갠다 | SCENARIO §9.4 |
| D48 | `hasRealData` 전환은 **S9 공유 직전에 사용자에게 묻는다.** 안전모드·백업·개인정보방침이 한 사건으로 켜진다 | SCENARIO §9.5 |
| D49 | `devoffice list` / `new` — **여러 제품**을 다룬다 | SCENARIO §9.6 |
| D50 | 방향 전환 요청에는 **"만든 게 없어지나요?"에 먼저 답한다** | SCENARIO §9.7 |
| D51 | 무료 한도 **80% 도달 시 선제 알림.** 초과하면 사용자는 고장인 줄 안다 | SCENARIO §9.8 |
| **D52** | **기능 크기 상한 — AC 5개 / 작업 8개 / 30분.** 넘으면 CTO가 쪼개고 사용자에게 알린다 | PROTOCOL §11.2 |
| **D53** | **미검증 기능이 있으면 다음 작업이 차단된다.** 경고가 아니라 차단. 예외 없음 | PROTOCOL §11.3 |
| **D54** | 만들어도 되는 것 3분류 — A(요청) 만든다 / B(필수 전제) 명시하고 만든다 / **C(있으면 좋은 것) 만들지 않는다** | PROTOCOL §11.4 |
| **D55** | 명세에 **"이번에 안 만드는 것"** 절 필수. 여기 적힌 걸 만들면 L3에서 범위 위반으로 잡힌다 | PROTOCOL §11.5 |
| D56 | 기반 설비·시드 데이터·테스트 계정도 **만들었으면 알린다** | PROTOCOL §11.6 |
| D57 | 실 서비스 전환 시 **시드 데이터 정리를 반드시 묻는다** | PROTOCOL §11.6 |

### 남은 열린 질문

1. **런처와 내부 도구의 이름 분리** — 둘 다 `devoffice`면 혼동된다.
   **셸 런처 `devoffice` / 세션 내 도구 `dof`**로 나누는 안을 권장 (SCENARIO §9.1)
2. **`both` 프리셋의 v0.1 포함 여부** — 3지선다를 v0.1부터 다 지원할지,
   웹·앱 단일 선택만 먼저 낼지. (모노레포 스캐폴딩이 v0.1을 눈에 띄게 무겁게 만든다)
3. **`bin/devoffice` 구현 언어** — Node(프로젝트와 동일 런타임, 사용자가 이미 설치) vs
   셸(의존성 0이지만 Windows 지원이 어려움). Node 권장
4. **에이전트 의인화** — 역할명으로 갈 것인가, 이름·캐릭터를 줄 것인가.
   `name` 프론트매터는 영문 kebab-case 필수이므로 **식별자는 역할명 / 표시 이름만 한국어**가 절충안
5. **`research` 실호출의 권한 경계** — 외부 API를 실제로 호출할 때 사용자 승인을 매번 받을지,
   읽기 전용 GET만 자동 허용할지
