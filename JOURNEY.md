# DevOffice 사용자 여정 설계

> 지금까지의 문서는 전부 **"개발하는 동안"**을 다뤘다.
> 이 문서는 그 **앞과 뒤**, 그리고 **끝까지 가게 만드는 것**을 다룬다.

---

## 1. 우리 설계는 여정의 가운데만 다루고 있었다

```
     ┌──────────────┬─────────────────────────────┬──────────────────┐
     │  시작 이전    │        개발 (S0~S7)          │   완성 이후       │
     ├──────────────┼─────────────────────────────┼──────────────────┤
     │  ✗ 비어 있음  │  ✅ DESIGN·DIALOGUE·        │  ✗ 비어 있음      │
     │              │     PROTOCOL·DEPLOY          │                  │
     │ Claude Code  │                              │ 사람들에게 알리기 │
     │ 설치·터미널  │                              │ 문의·버그 신고    │
     │ 폴더 개념    │                              │ 모니터링          │
     │ 플러그인 설치│                              │ 유지보수·업데이트 │
     │ 첫 5분       │                              │ 성장·이관         │
     └──────────────┴─────────────────────────────┴──────────────────┘
              ↑                                              ↑
       가장 높은 허들                              사용자의 진짜 목표
```

두 가지가 심각하다.

- **왼쪽**: 이탈이 가장 많이 일어나는 구간인데 설계가 없다. 여기를 못 넘으면 나머지는 존재하지 않는 것과 같다
- **오른쪽**: 사용자의 목표는 *코드를 만드는 것*이 아니라 **제품을 운영하는 것**이다.
  "다 만들었습니다"에서 끝나면 제품이 아니라 폴더를 준 것이다

---

## 2. CLI를 비개발자가 쓸 수 있게 만들기

### 2.0 대전제: Phase 1은 CLI로 완결된다

**이건 양보하지 않는다.** 다른 표면(데스크톱 앱 등)으로 우회해서 UX 문제를 푸는 것은
[DESIGN §8](./DESIGN.md)의 headless-first 원칙 위반이다 —
*플러그인은 GUI 없이 CLI만으로 완결되어야 하고, GUI는 뷰어 + 리모컨이다.*

우회가 나쁜 이유는 세 가지다:

1. **우리가 통제하지 않는 표면에 UX를 의존시킨다.** 그 표면이 바뀌면 우리 설계가 무너진다
2. **CLI에서 되면 어디서든 된다.** 반대는 성립하지 않는다
3. **Phase 2 GUI의 존재 이유가 사라진다.** 다른 앱이 문제를 풀어준다면 GUI를 만들 이유가 없다

따라서 문제를 이렇게 다시 세운다:

> **Phase 1 (CLI)** — 비개발자가 **가능한** 수준으로
> **Phase 2 (GUI)** — 비개발자가 **편안한** 수준으로

그리고 **Phase 1에서 만드는 출력 규격이 그대로 Phase 2 GUI의 화면 명세가 된다.**
`state.json`이 데이터라면, 출력 규격은 **화면 설계**다. 둘 다 Phase 2로 이어진다.

### 2.1 진입 — "문서"가 아니라 "복사할 한 줄"

비개발자에게 "Node를 설치하고, npm으로 전역 설치하고, PATH를 확인하고…"는 성립하지 않는다.
설치 안내가 문단이면 이미 실패다.

**설치 스크립트 한 줄로 전부 압축한다.**

```bash
curl -fsSL https://devoffice.sh/install | sh
```

이 한 줄이 하는 일:

```
① Node 확인 → 없으면 설치
② Claude Code 설치
③ 플러그인 마켓플레이스 추가 + devoffice 설치
④ ~/Documents/DevOffice/ 생성
⑤ 다음에 무엇을 하면 되는지 3줄로 안내
```

**사용자가 하는 일: 터미널을 열고, 한 줄 붙여넣고, Enter.**

남는 건 *터미널을 여는 법* 하나뿐이고, 그건 스크린샷 3장으로 끝난다
(macOS: Spotlight에서 "터미널" / Windows: 시작 메뉴에서 "PowerShell").
**README에서 가장 많이 읽히는 부분이 이 3장의 스크린샷이 된다.**

> **Windows에 대한 정직한 범위:** v0.1은 macOS 우선이다.
> Windows(PowerShell)는 v0.2. 설치 스크립트와 `bin/devoffice`의 경로·권한 처리가
> 근본적으로 다르고, 어설프게 지원하면 오히려 이탈이 는다.

### 2.2 "폴더 = 프로젝트"를 한 번만 가르친다

비개발자에게 파일 시스템은 약한 개념이다. 그런데 딱 하나는 이해해야 한다:
**폴더 하나가 제품 하나다.**

- 위치를 우리가 정해준다: `~/Documents/DevOffice/<제품이름>`
- 사용자가 고르게 하지 않는다. "여기에 만들게요"라고 통보한다
- 이 폴더를 **"제품 상자"**라고 부른다. 안의 파일 구조는 설명하지 않는다
- 백업·이사·삭제가 전부 이 폴더 단위라는 것만 알려준다

### 2.3 첫 5분 — 사용자가 시작을 트리거한다

플러그인을 설치한 직후 사용자는 **무엇을 쳐야 하는지 모른다.**
그래서 한때 `SessionStart` 훅이 스스로 대화를 여는 방식을 택했었다.

**그 방식은 철회했다.** 이유:

- 플러그인이 세션을 가로채면 **사용자가 통제권을 잃는다.** 이 폴더에서 다른 일을
  하려던 사용자에게도 말을 걸게 된다
- 시작 지점이 불분명해진다. 언제 시작됐는지 사용자가 모른다
- 훅이 세션마다 컨텍스트를 밀어넣는 비용이 계속 든다

> **플러그인은 먼저 말을 걸지 않는다. 사용자가 `devoffice` 스킬을 불러야 시작된다.**

대신 **진입 안내를 install.sh 출력과 README가 책임진다.**
설치 스크립트가 끝날 때 다음에 무엇을 하면 되는지 알려주고,
그 한 번만 넘기면 이후로는 `devoffice` 하나만 기억하면 된다.

**이어하기도 스킬이 한다.** 스킬이 불리면 `devoffice state show` 로 상태를 읽고,
며칠 만이면 만든 것과 배포 주소를 함께 보여준다 (§4.3).

### 2.4 진입 완료 정의

| # | 사용자가 하는 일 | 소요 |
|---|---|---|
| 1 | 터미널 열기 (스크린샷 3장) | 1분 |
| 2 | 설치 한 줄 붙여넣고 Enter | 3분 (대부분 대기) |
| 3 | Claude 로그인 (브라우저 승인) | 2분 |
| 4 | `claude` 실행 후 `/devoffice` 호출 | 즉시 |

**6분, 사용자가 타이핑하는 것은 한 줄.**
여기를 넘긴 사용자만 우리 제품을 만난다.

---

### 2.5 CLI에서도 "화면"은 만들 수 있다

여기서부터가 CLI 전제의 핵심이다. **텍스트만 있어도 화면은 설계할 수 있다.**
그런데 기본 상태의 Claude Code 출력은 개발자용이다 — 비개발자에게는
도구 호출 로그와 코드 블록이 **노이즈이자 공포**다.

플러그인이 통제할 수 있는 것은 생각보다 많다:

| 수단 | 용도 |
|---|---|
| `output-styles/` | **출력 규격을 강제**하는 플러그인 컴포넌트 |
| 스킬·에이전트 프롬프트 | 프로듀서가 말하는 방식 전부 |
| `settings.json`의 `subagentStatusLine` | 대기 중 "지금 뭐 하는 중"을 사람 말로 |
| 훅 | 오류 번역, 세션 시작/종료 시점의 안내 |
| `bin/devoffice` | 정형화된 화면 출력 |

### 2.6 출력 규격 6원칙

1. **한 화면에 하나의 결정.** 스크롤이 필요하면 이미 실패다
2. **항상 "지금 어디" + "다음에 뭐"로 끝난다.** 사용자가 다음 행동을 몰라 멈추는 일이 없어야 한다
3. **구분선과 여백으로 계층을 만든다.** 텍스트 벽 금지
4. **이모지는 상태 표시로만.** ✅ ⏳ ❌ 🔗 — 장식용 금지
5. **기술 용어 0개.** 나오면 즉시 번역한다
6. **한 응답 15줄 이내.** 넘으면 파일로 빼고 "열어보시겠어요?"

**표준 출력 형태:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  💬 댓글 기능                  3/12
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ✅ 만들었어요
  🔗 https://myapp.vercel.app/posts/1

  이 3가지만 확인해주세요
   1. 댓글을 하나 써보세요
   2. 방금 쓴 댓글을 지워보세요
   3. 로그아웃하고 댓글창이 사라지는지

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  잘 되면 "네", 이상하면 뭐가 이상한지 말씀해주세요
```

**마지막 줄이 규격에서 가장 중요하다.** CLI에는 버튼이 없으므로
*무엇을 입력해야 하는지*를 매번 알려주지 않으면 사용자가 그 자리에서 멈춘다.

### 2.7 침묵 — CLI의 가장 큰 취약점

에이전트가 3분 도는 동안 화면이 멈춘 것처럼 보인다. 비개발자는 **"고장났나?"** 한다.
이건 GUI라면 스피너 하나로 끝날 문제지만 CLI에서는 설계가 필요하다.

**세 겹으로 대응한다:**

| 겹 | 방법 |
|---|---|
| **예고** | 시작 전에 말한다: "이제 서버 쪽을 만들게요. **2~3분 걸리고, 화면이 조용해도 정상이에요.**" |
| **상태줄** | `subagentStatusLine`에 사람 말로: `서버 만드는 중… (2/3)` |
| **분할** | 5분짜리 에이전트 하나보다 **2분짜리 셋**이 낫다. 중간 보고가 생긴다 |

> **"화면이 조용해도 정상이에요"라는 한 줄이 실제로 큰 차이를 만든다.**
> 예고된 침묵은 불안이 아니다. 예고 없는 침묵만 불안이다.

### 2.8 오류 — 숨기지 말고 해석을 붙인다

CLI에서는 Claude Code가 도구 오류를 화면에 그대로 뿌린다.
`PostToolUseFailure` 훅으로 번역해도 **원본은 이미 보인 뒤**다.

→ 숨기려 하지 않는다. **바로 아래에 해석을 붙이고, 무시해도 된다고 말한다.**

```
  ⚠️  위에 빨간 글씨가 보이시죠? 무시하셔도 돼요.

  무슨 일이냐면: 데이터를 저장할 공간이 아직 준비가 안 됐어요.
  지금 할 일:   제가 준비할게요. 30초만요.
```

**"무시하셔도 돼요"가 핵심이다.** 비개발자는 빨간 글씨를 보면
자기가 뭘 잘못했다고 생각하고 멈춘다.

### 2.9 읽기는 브라우저로 뺀다 — Phase 2로 가는 다리

PRD·명세·검증 시나리오는 사용자가 **읽어야 하는** 문서다.
터미널에서 마크다운을 읽게 하는 것은 나쁘다.

```
devoffice open prd        마크다운을 HTML로 렌더해 브라우저에서 연다
devoffice open status     현재 상태 한 화면
```

**이것이 Phase 2 GUI의 최초 형태다.** 정적 HTML 한 장에서 시작해
파일 감시 + 상호작용으로 자라난다. Phase 1에서 억지로 참다가
Phase 2에서 새로 만드는 게 아니라, **같은 것이 자란다.**

---

## 3. Phase 3 — 완성 이후 (사용자의 진짜 목표)

기능을 다 만든 사용자는 이제 **개발자가 아니라 서비스 운영자**가 된다.
여기서 필요한 것은 우리가 전혀 설계하지 않은 종류의 도움이다.

### 3.1 단계 확장

기존 S0~S8에 이어붙인다.

| 단계 | 이름 | 내용 |
|---|---|---|
| **S9** | **알리기** | 도메인 연결, 링크 미리보기(OG), 검색 노출, 공유용 소개 문구 |
| **S10** | **듣기** | 문의 받는 창구, 버그 신고 경로, 사용 현황 보기 |
| **S11** | **고치기** | 신고 → 재현 → 수정 → 배포. 개발 루프의 축약판 |
| **S12** | **지키기** | 의존성 업데이트, 보안 패치, 결제수단·도메인 만료, 백업 확인 |

### 3.2 S9 — 링크를 보냈는데 아무것도 안 뜬다

비개발자가 제품을 완성한 뒤 **가장 먼저 하는 행동은 링크를 카톡에 보내는 것**이다.
그때 미리보기 이미지가 안 뜨면 제품이 초라해 보인다. 이건 기능 문제가 아니라 **첫인상 문제**다.

`/devoffice:launch` 체크리스트:

- [ ] 도메인 연결 (없으면 기본 주소 그대로 — 괜찮다고 알려준다)
- [ ] **OG 태그** — 링크 공유 시 제목·설명·이미지
- [ ] 파비콘 (탭 아이콘)
- [ ] 검색 노출 설정 (robots, sitemap)
- [ ] **모바일에서 열어보기** ← 비개발자는 데스크탑에서만 확인한다
- [ ] 첫 화면에 "이게 무슨 서비스인지" 한 줄

**마지막 항목이 자주 빠진다.** 만든 사람은 아는데 처음 온 사람은 모른다.

### 3.3 S10 — 사용자가 에러를 만나도 우리는 모른다

비개발자 제품의 가장 흔한 실패: **누군가 에러를 만났는데 아무도 모른다.**
그 사람은 그냥 떠나고, 만든 사람은 "왜 아무도 안 쓰지"라고 생각한다.

세 가지를 기본으로 넣는다:

| 장치 | 목적 | 비개발자에게 보이는 형태 |
|---|---|---|
| **에러 수집** | 터진 걸 안다 | "어제 3명이 오류를 만났어요. 원인은 ○○입니다" |
| **문의 창구** | 말할 곳을 만든다 | 화면 구석 "문의하기" 버튼 |
| **사용 현황** | 쓰이는지 안다 | "이번 주 방문 42명, 가입 5명" |

이건 **기능이 아니라 기반 설비**다. 골격(S4) 단계에서 미리 넣어두는 것이
나중에 붙이는 것보다 훨씬 싸다. → S4 체크리스트에 추가한다.

### 3.4 S11 — 신고를 수정으로 바꾸는 루프

사용자가 "안 돼요"라고 하면 비개발자는 재현하지 못한다.
**에러 수집이 있으면 재현이 필요 없다** — 어떤 화면에서 어떤 오류가 났는지 이미 있다.

```
문의 접수 → 에러 로그 대조 → 재현 가능? →  기능 루프의 축약판
                                            (명세 없이 spec = 신고 내용)
```

이 루프가 있어야 제품이 **살아 있는 것**이 된다. 없으면 한 번 만들고 끝난 폴더다.

### 3.5 S12 — 몇 달 뒤에도 돌아가게

비개발자가 겪는 전형적 사고:

- 3개월 뒤 열었더니 안 돌아감 (의존성·Node 버전)
- 도메인이 만료됨
- 결제 카드가 만료되어 서버가 내려감
- 보안 취약점이 몇 달째 방치

**대응:**

- 버전 잠금 (`package-lock.json` 커밋, Node 버전 고정 파일)
- `/devoffice:checkup` — 한 달에 한 번 돌리는 점검 명령
  - 의존성 보안 경고, 만료 임박 항목, 백업 상태, 비용 추이
- **`Stop` 훅으로 마지막 점검 이후 30일이 지나면 알린다** — 사용자는 먼저 안 한다

---

## 4. 완주 설계 — 왜 그만두는가

기술이 아니라 사람의 문제다. 그리고 **완주율이 이 제품의 진짜 지표**다.

### 4.1 끝이 안 보이면 그만둔다

비개발자는 남은 양을 가늠하지 못한다. 매 기능 완료 시 자동으로 보여준다:

```
✅ 댓글 기능 완료 — https://myapp.vercel.app 에서 바로 보실 수 있어요

   ▓▓▓▓▓░░░░░░░  12개 중 5개 완료
   다음: 좋아요 기능 (20분쯤)
   남은 기능 7개 · 이 속도면 3~4일쯤이면 다 만들어져요
```

**"이 속도면 언제 끝난다"가 핵심이다.** 진척 막대만으로는 부족하다.

### 4.2 기대는 처음에 관리한다

PRD 승인 시점에 한 번 정직하게 말한다:

> "말씀하신 걸 만들려면 기능이 12개쯤 됩니다.
> 하나에 20~30분씩 걸리니, 하루 2~3개씩 하시면 **일주일 정도** 걸려요.
> 한 번에 다 하실 필요 없고, 언제든 멈췄다가 이어서 하실 수 있어요."

이 말이 없으면 3일째에 "왜 이렇게 오래 걸리지"라고 생각한다.
**있으면 3일째는 예정대로다.**

### 4.3 중단 후 복귀 — 사용자는 자기 프로젝트를 잊는다

스킬이 상태를 읽는 건 **에이전트를 위한 것**이다.
2주 만에 돌아온 **사용자 본인**도 잊었다.

```
2주 만이시네요. 지금까지 이렇게 만들어두셨어요:

  ✅ 로그인 · 글쓰기 · 글 목록 · 댓글 · 좋아요
  🔗 https://myapp.vercel.app        ← 한번 열어보고 오세요

  다음에 하려던 건 [검색 기능]이었어요. 이어서 할까요?
```

**만든 것을 직접 보여주는 것**이 어떤 요약보다 효과적이다.
자기가 만든 게 살아 있는 걸 보면 다시 하고 싶어진다.

### 4.4 작은 성공의 리듬

이미 설계에 있다(기능마다 배포). 다만 **첫 기능까지가 여전히 멀다.**

- S4(Hello World 배포)와 첫 기능 완료 사이가 최대 공백 구간
- → **백로그 1번은 가장 쉽고 눈에 보이는 기능으로 정렬한다.**
  기술적 의존성만 보면 보통 인증이 먼저지만, **첫 기능은 성취감 기준으로 고른다**

이게 CTO의 백로그 정렬 규칙에 들어가야 한다: **1번은 난이도가 아니라 가시성으로 고른다.**

### 4.5 "내가 만들었다"는 감각

비개발자가 이 제품을 계속 쓰게 만드는 건 결과물이 아니라 **소유감**이다.

- 결정한 것을 기록하고 보여준다 — 가정 명세(DIALOGUE §3)가 이미 이 역할을 한다
- `/devoffice:explain` — "이 화면은 어떻게 동작하나요?"를 쉬운 말로
- 완성 시 **제품 소개 페이지**를 자동 생성 (만든 기능 목록, 개발 기간)

---

## 5. 비용 가시화 — 모르면 무섭고, 알면 결정할 수 있다

비개발자는 두 종류의 비용을 만나는데 **둘 다 예측하지 못한다.**

| | 예측 방법 | 사용자에게 보이는 형태 |
|---|---|---|
| **AI 사용료** | 기능당 대략치를 누적 | "지금까지 기능 5개 · 사용량 약 ○○" |
| **인프라 요금** | 선택한 서비스의 요금제 | **`.devoffice/costs.md` 월 예상 비용표** |

**기능을 추가할 때 비용이 늘면 미리 말한다:**

> "이 기능에는 이메일 발송이 필요해서 월 ○원이 추가돼요.
> ① 그대로 진행 ② 무료 한도(월 100통)로 시작 ← 추천 ③ 이 기능 보류"

**비용을 승인 대상으로 만드는 것**이 중요하다. 나중에 청구서로 알게 되면
그 사용자는 다시 안 돌아온다.

---

## 6. 개발이 아닌데 막히는 것들

제품을 100% 완성하려면 코드 밖의 것들이 필요한데, **비개발자는 여기서도 막힌다.**
그런데 우리 설계에는 아무 언급이 없다.

| 필요한 것 | 없으면 | 우리의 대응 |
|---|---|---|
| **로고·아이콘** | 완성해도 초라함 | 텍스트 기반 임시 로고 자동 생성 + 나중에 교체 안내 |
| **화면 문구** | 빈칸투성이 | 에이전트가 초안 작성 → 사용자가 고침 |
| **실제 콘텐츠** | **다 만들었는데 넣을 게 없음** | **PRD 단계에서 미리 알린다** |
| **도메인 이름** | 결정 못 해서 멈춤 | 후보 3개 제안 + 등록 가능 여부 확인 |
| **소개 문구** | 공유해도 설명 못 함 | PRD의 한 줄 정의를 재사용 |

**세 번째가 실제로 가장 자주 막는다.** 쇼핑몰을 만들었는데 상품 정보가 없고,
블로그를 만들었는데 쓸 글이 없다.

> PRD 승인 시점에 알린다: "이 제품에는 [상품 사진과 설명]이 필요해요.
> 준비되면 알려주세요 — 없어도 개발은 진행할 수 있고, 임시 데이터로 채워둘게요."

**시드 데이터**도 여기 속한다. 빈 목록만 있는 제품은 완성돼도 죽어 보인다.
검증 단계에서 그럴듯한 임시 데이터를 넣어주는 것이 기본이어야 한다.

---

## 7. 확장된 전체 단계

| | 단계 | 산출물 |
|---|---|---|
| **S-1** | **진입** | 터미널 열기 · **설치 한 줄** · `/devoffice` 호출 |
| S0 | 인테이크 | 아이디어 대화 |
| S1 | PRD | 제품 정의서 + **기대 관리** + **콘텐츠 준비 안내** |
| S2 | 설계 | 스택·백엔드 형태 잠금 |
| S3 | 환경 | 계정·설치 |
| S4 | 골격 | Hello World 배포 + **에러 수집·문의 창구 기반 설비** |
| S5 | 백로그 | 기능 목록 (**1번은 가시성 기준으로**) |
| S6 | 기능 루프 | 반복 + **매회 진척도 표시** |
| S7 | 운영 | 수정·추가·롤백 |
| S8 | 출시 | 앱스토어 (해당 시) |
| **S9** | **알리기** | 도메인·OG·검색·모바일 확인 |
| **S10** | **듣기** | 에러 수집·문의·사용 현황 |
| **S11** | **고치기** | 신고 → 수정 루프 |
| **S12** | **지키기** | 월간 점검·업데이트·만료 관리 |

---

## 8. 로드맵 반영

| 버전 | 추가 |
|---|---|
| **v0.1** | **S-1(진입)** — 설치 스크립트 한 줄, 터미널 열기 스크린샷, 설치 후 안내 (macOS) |
| **v0.1** | **출력 규격(§2.6) + 침묵 대응(§2.7) + 오류 해석(§2.8)** — CLI 사용성의 최소 골격 |
| v0.2 | Windows(PowerShell) · 진척도 표시 · 기대 관리 · 복귀 요약 · `devoffice open` |
| v0.3 | S4에 기반 설비(에러 수집·문의) 포함 |
| v0.4 | S9~S11 (알리기·듣기·고치기) |
| v0.5 | S12 점검, 비용 가시화, 비개발 산출물 지원 |

> **두 가지가 이 문서의 결론이다.**
>
> 1. **S-1을 v0.1로 올린다.** "PRD → Hello World"가 아무리 좋아도 그 앞에 도달하지 못하면 의미가 없다
> 2. **출력 규격을 v0.1에 넣는다.** CLI에서 비개발자 사용성은 *나중에 다듬는 것*이 아니라
>    **첫 버전의 기능 그 자체**다. 그리고 이 규격이 Phase 2 GUI의 화면 명세가 된다
