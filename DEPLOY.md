# DevOffice 배포·원격 운영 설계

> 비개발자가 실제로 통과할 수 있는 배포 경로를 만드는 문제.
> `DESIGN.md`(무엇을) · `PROTOCOL.md`(어떻게 일하는가)에 이어, 이 문서는 **어디에 올리는가**를 다룬다.

---

## 1. 우려의 연쇄

```
서버가 필요하다
  → EC2를 쓰기로 한다
    → EC2 구축이 어렵다                          ①
      → 로컬 코드를 EC2에 어떻게 올리지?          ②
        → GitHub 사용법을 가르쳐야 한다            ③
          → EC2에서 문제가 생기면
            로컬 Claude 세션이 원인을 못 찾는다    ④
              → EC2에 Claude 세션을 열어야 한다     ⑤
```

이 연쇄를 하나씩 풀기 전에, 먼저 물어야 할 것이 있다.

### 이 연쇄는 어디서 시작되는가

**②③④⑤는 전부 ①의 파생물이고, ①은 `server-vps`를 선택했을 때만 발생한다.**

그리고 비개발자가 EC2를 요구하는 실제 이유를 보면 대부분 **필요가 아니라 선호 또는 오해**다:

| 실제 이유 | 정말 EC2가 필요한가 |
|---|---|
| "AWS가 표준이라고 들었다" | ❌ |
| "서버가 있어야 제대로 된 것 같다" | ❌ |
| "나중에 커지면 필요할 것 같다" | ❌ (그때 옮기면 된다) |
| 사내망 접근, 특수 소프트웨어, GPU | ✅ |
| 회사가 이미 AWS를 쓴다 | ✅ |

**그렇다고 "안 됩니다"라고 막으면 안 된다.** 사용자가 재차 원하면 그건 사용자의 결정이다.
전략은 **막는 것이 아니라 미루는 것 + 사다리를 놓는 것**이다.

---

## 2. 원칙: 배포는 사다리다

> **첫 칸에서 시작한다. 필요할 때만 올라간다. 각 칸은 앞 칸의 결과물을 버리지 않는다.**

| 칸 | 배포 대상 | 사용자가 하는 일 | 언제 |
|---|---|---|---|
| **1** | Vercel / Expo | 계정 + 버튼 클릭 | **기본값. v0.1** |
| **2** | + Supabase | 계정 + 프로젝트 생성 | DB·인증이 필요할 때 |
| **3** | Railway / Render / Fly | 계정 + 카드 + GitHub 연결 | **상시 서버**가 필요할 때 |
| **4** | **AWS Lightsail** | AWS 계정 + 콘솔 클릭 ~5회 | "AWS를 쓰고 싶다"의 답 |
| **5** | AWS EC2 | AWS 계정 + 콘솔 클릭 ~10회 | 완전한 제어가 **정말** 필요할 때 |

**핵심은 칸 사이 이동 비용이 낮다는 것이다.** 코드는 그대로고 배포 대상만 바뀐다.
그래서 이렇게 말할 수 있다:

> "EC2로 갈 수 있는데, 먼저 3칸(Railway)으로 띄워서 앱이 도는 걸 확인하고
> 실제로 부족하면 옮겨드릴게요. 코드는 하나도 안 바뀝니다."

대부분은 옮기지 않게 된다. 그리고 옮길 때는 **이미 돌아가는 앱이 있으므로**
EC2 설정 실패의 위험이 훨씬 낮다 — 비교 대상이 있고, 실패해도 돌아갈 곳이 있다.

> **"먼저 되게 만들고 나중에 옮긴다"가 "처음부터 어려운 걸 한다"보다 압도적으로 낫다.**

### 4칸에 Lightsail을 넣은 이유

"AWS를 쓰고 싶다"는 요구에 EC2를 바로 주는 것은 나쁜 답이다. **Lightsail이 훨씬 나은 답이다.**

| | Lightsail | EC2 |
|---|---|---|
| 요금 | **월 $5 고정** | 사용량 과금 — **요금 폭탄 위험** |
| 콘솔 | 단순 (선택지 몇 개) | 복잡 (VPC·서브넷·AMI·IAM…) |
| 방화벽 | 체크박스 | 보안 그룹 규칙 |
| SSH | **브라우저에서 바로 접속 가능** | 키 파일 필요 |
| 이전 | 나중에 EC2로 옮길 수 있음 | — |

**고정 요금이 결정적이다.** 비개발자에게 가장 큰 실질적 피해는 예상치 못한 청구서인데,
Lightsail은 구조적으로 그 위험이 낮다. 그리고 AWS를 쓴다는 요구도 만족시킨다.

---

## 3. ②③ 풀기 — GitHub 사용법을 가르치지 않는다

### 3.1 가이드가 필요하다는 것 자체가 설계 실패 신호다

"GitHub 사용법 가이드를 만들어야 한다"는 결론에 도달했다면, 그건 **사용자에게 git을
의식하게 만들었다는 뜻**이다. 원칙 P7에서 이미 정했다 — *사용자는 git을 모른 채
`/devoffice:undo`만 안다.* 배포도 같아야 한다.

**사용자가 GitHub에서 실제로 해야 하는 일은 두 가지뿐이다:**

1. 계정 생성 (이메일 인증)
2. `gh auth login` → 브라우저에서 코드 입력 후 승인 버튼

그게 전부다. 저장소 생성(`gh repo create`), 커밋, 푸시는 전부 에이전트가 실행한다.
사용자가 알아야 할 개념은 **"내 코드가 저장되는 인터넷 금고"** 하나다.

> `gh auth login`의 브라우저 승인 방식은 SSH 키 생성·등록보다 비개발자에게 훨씬 쉽다.
> SSH 키 경로를 절대 사용자에게 노출하지 않는다.

### 3.2 ②의 진짜 답 — GitHub과 배포를 분리한다

"로컬 코드를 EC2에 어떻게 올리지?"에 GitHub으로 답하면 연쇄가 시작된다.
**두 관심사를 분리하면 연쇄가 끊긴다.**

| 관심사 | 수단 | 사용자가 아는 것 |
|---|---|---|
| **백업 · 되돌리기** | GitHub (에이전트가 자동 커밋·푸시) | `/devoffice:undo` |
| **서버에 올리기** | **rsync/scp로 로컬 → 서버 직접 전송** | `/devoffice:ship` |

서버는 GitHub을 전혀 모른다. 따라서:

- EC2에 GitHub 자격증명을 넣을 필요가 없다 (deploy key·PAT 불필요)
- GitHub Secrets에 SSH 개인키를 붙여넣을 필요가 없다 (**위험한 작업 제거**)
- GitHub Actions 워크플로 설정이 필요 없다
- **③이 통째로 사라진다**

```
로컬 ──git push──→ GitHub   (금고. 서버와 무관)
  │
  └──rsync────────→ 서버     (배포)
```

**트레이드오프:** 로컬이 진실의 원천이 되고, 여러 기기에서 작업할 수 없다.
비개발자 1인 프로젝트에서는 문제가 아니다. 팀이 생기거나 CI가 필요해지면
그때 GitHub Actions로 승급한다 — 그 시점의 사용자는 이미 비개발자가 아니다.

---

## 4. ① 풀기 — 자동화 경계선을 정확히 긋는다

`infra` 에이전트가 "절차서를 생성한다"만으로는 부족하다. **사람이 절차서를 따라 하면 반드시 틀린다.**
따라서 사람이 해야만 하는 구간을 최소로 압축하고, 나머지는 전부 스크립트로 만든다.

| 구간 | 자동화 | 방법 |
|---|---|---|
| AWS 계정 생성 | ❌ | 콘솔 안내 (§6) |
| 결제수단 등록 | ❌ | 콘솔 안내 |
| **예산 알림 설정** | ❌ | 콘솔 안내 — **인스턴스 생성보다 먼저** |
| 인스턴스 생성 | ❌ | 콘솔 안내 (Lightsail이면 클릭 ~5회) |
| 키 파일 받기 | ❌ | 저장 위치 지정 + 권한 설정(`chmod 600`)은 자동 |
| 방화벽 포트 | ❌ | 콘솔 안내 |
| ──────────── | | **여기까지가 사람의 몫** |
| SSH 접속 확인 | ✅ | `devoffice remote doctor` |
| **서버 초기 세팅 전부** | ✅ | **`devoffice remote bootstrap` 명령 1개** |
| 도메인·SSL | ✅ | 스크립트 (DNS 레코드 등록만 사람) |
| 배포 | ✅ | `devoffice remote deploy` |
| 운영·디버깅 | ✅ | `devoffice remote *` (§5) |

### 4.1 부트스트랩 — 20단계 가이드를 명령 1개로

SSH 접속이 되는 순간부터는 전부 자동이다. 이게 이 설계에서 가장 중요한 결정이다.

```
devoffice remote bootstrap
  ├─ 스왑 메모리 생성          ★ 1GB RAM 인스턴스에서 빌드 시 OOM 방지
  ├─ Node LTS 설치
  ├─ 방화벽 (ufw) 설정
  ├─ nginx 리버스 프록시
  ├─ certbot + 자동 갱신
  ├─ systemd 서비스 등록       ★ 재부팅 후 자동 시작
  ├─ 로그 로테이션              ★ 디스크 참 방지
  ├─ 배포 디렉토리 구조
  └─ 상태 리포트 출력
```

> ★ 표시된 셋은 **비개발자가 절대 스스로 찾아낼 수 없는데 반드시 터지는** 문제들이다.
> 1GB 인스턴스에서 `npm run build`는 스왑 없이 죽고, systemd 등록이 없으면 재부팅 후
> 조용히 사라지며, 로그 로테이션이 없으면 몇 주 뒤 디스크가 찬다.
> 부트스트랩이 이 셋을 기본으로 처리하는 것만으로도 장기 실패의 큰 몫이 사라진다.

### 4.2 AWS CLI 자동화를 기본으로 하지 않는 이유

인스턴스 생성까지 CLI로 자동화할 수 있지만, 그러려면 IAM 사용자와 액세스 키가 필요하다.

- IAM 사용자 생성 + 정책 범위 설정은 **콘솔 클릭보다 어렵다**
- 액세스 키는 유출 시 **요금 폭탄으로 직결**된다. 비개발자에게 쥐여주기 위험하다

→ **기본은 콘솔 클릭.** CLI 자동화는 옵션으로 제공하되 권장하지 않는다.

---

## 5. ④ 풀기 — 로컬 세션에서 원격을 다룬다

### 5.1 왜 "EC2에 Claude를 여는 것"이 기본이 되면 안 되는가

지적하신 대로 원격 세션이 서버 내부를 가장 잘 본다. 하지만 기본값으로는 부적합하다:

| 문제 | 설명 |
|---|---|
| **메모리** | 프리티어 t2.micro(1GB)에서 Claude Code + 앱 빌드는 OOM. 결정적 |
| **인증** | EC2 안에서 브라우저 로그인이 어렵다. API 키를 서버에 두는 것도 부담 |
| **컨텍스트 분리** | 세션이 둘로 갈라지면 C2 문제(기억 단절)가 세션 레벨에서 재발한다 |
| **사용자 부담** | 비개발자가 터미널 두 개를 오가야 한다 |

**가장 중요한 건 세 번째다.** 디버깅은 *코드와 증상을 대조하는 일*인데,
로컬(코드)과 원격(증상)이 다른 세션에 있으면 **양쪽 다 반쪽만 본다.**
하나의 세션이 코드와 서버 상태를 동시에 보는 편이 진단에 유리하다.

### 5.2 답: 로컬 세션이 SSH로 원격을 조작한다

에이전트는 Bash 도구로 SSH 명령을 실행할 수 있다. 이걸 **얇은 명령 레이어로 규격화**한다.

```
devoffice remote doctor          접속·환경 점검 (멱등)
devoffice remote bootstrap       초기 세팅 (§4.1)
devoffice remote deploy          rsync 전송 + 빌드 + 재시작
devoffice remote logs [--since]  애플리케이션·nginx 로그
devoffice remote status          프로세스·포트·디스크·메모리
devoffice remote snapshot        상태 전체를 파일로 수집 (§5.3)
devoffice remote rollback        직전 배포로 되돌리기
devoffice remote shell "<cmd>"   임의 명령 (에이전트 전용)
devoffice remote destroy         정리 안내 (요금 중단)
```

접속 정보는 `.devoffice/infra/connection.json`에 **키 파일 경로만** 둔다(키 자체는 아님).
에이전트는 SSH 문법을 알 필요가 없고, 사용자는 SSH의 존재를 알 필요가 없다.

**디버깅에 필요한 것은 대화형 셸이 아니라 로그와 상태 조회다.**
그래서 이 명령들만으로 ④의 대부분이 해소된다.

### 5.3 스냅샷 — 원격 디버깅을 파일 비교로 환원한다

SSH 래퍼를 넘어서는 한 걸음. **서버가 자기 상태를 통째로 뱉게 하고, 그걸 로컬 파일로 가져온다.**

```
devoffice remote snapshot
  → .devoffice/infra/snapshots/2026-08-10T14-30.md
     · 프로세스 / 포트 / 디스크 / 메모리
     · systemd 서비스 상태
     · 최근 애플리케이션 로그 200줄
     · nginx 설정과 에러 로그
     · 환경변수 "키 목록"만 (값은 절대 수집 안 함)
     · 배포된 커밋 해시
```

이렇게 하면:

- 로컬 세션이 원격 상태를 **파일로 읽는다** → 원칙 P2("파일이 조직의 기억")와 일치
- 세션이 끊겨도 증거가 남는다
- **배포 전/후 스냅샷을 비교할 수 있다** ← 원인 추적에 매우 강력
- Phase 2 GUI에서 서버 상태 화면으로 그대로 렌더링된다

**배포 실패 시 스냅샷을 자동 수집한다.** 그러면 사용자가 "안 돼요"라고만 말해도
에이전트는 이미 증거를 갖고 있다. 비개발자는 상황을 정확히 설명하지 못하므로,
**설명을 요구하지 않고 증거를 먼저 확보하는 구조**가 필요하다.

### 5.4 ⑤ — 원격 Claude는 "서브에이전트"로 쓴다 (고급)

원격 세션이 정당한 경우가 있다: 인스턴스가 충분히 크고(2GB+), 서버에서 장시간 작업이
필요하고, 로컬↔원격 왕복이 너무 잦을 때.

이때도 **세션을 둘로 나누지 않는다.** 대신 로컬 세션이 원격에서 headless Claude를 호출한다:

```bash
devoffice remote investigate "서버가 시작되지 않는 원인을 찾아 리포트만 작성"
  → 내부적으로: ssh ... "cd /srv/app && claude -p '...' --output-format json"
```

원격에 완전한 컨텍스트를 가진 에이전트가 잠깐 태어나 조사하고 **결과만 반환**한다.
구조적으로 서브에이전트와 동일하다 — 로컬 세션이 여전히 유일한 창구다.

전제: EC2에 Claude Code 설치 + 인증(API 키). 비용은 사용자 부담.
→ **v1.0 고급 옵션. 기본 경로가 아니다.**

---

## 6. 콘솔 안내 규격과 스크린샷 채널

### 6.1 안내는 4요소를 반드시 갖춘다

AWS 콘솔 안내는 `infra` 플레이북에서 형식을 고정한다.

```
① 어느 화면인가    "AWS 콘솔 상단 검색창에 'Lightsail' 입력 → 첫 결과 클릭"
② 어디를 보는가    "화면 가운데 주황색 '인스턴스 생성' 버튼"
③ 무엇을 하는가    "클릭 → 리전은 '서울', 이미지는 'Ubuntu 22.04' 선택"
④ 정상이면 무엇이 보이는가   "인스턴스 목록에 '보류 중'이 표시되면 정상입니다.
                              1~2분 뒤 '실행 중'으로 바뀝니다"
```

**④가 결정적이다.** 기대 상태를 알려주면 사용자가 스스로 검증할 수 있고,
"이게 맞나요?"라는 되물음이 사라진다. 그리고 다르면 즉시 알아챈다.

### 6.2 스크린샷 채널 — 비개발자 지원의 결정적 도구

비개발자는 화면 상태를 정확히 설명하지 못한다. "뭐가 보이세요?"로는 진단이 안 된다.

→ **막히면 곧바로 스크린샷을 요청한다.** Claude는 이미지를 읽는다.

> "지금 화면을 캡처해서 이 창에 붙여넣어 주세요. (Mac: `Cmd+Shift+4`)"

이건 이미 되는 기능이지만, **프로토콜에 명시적으로 넣지 않으면 실제로 쓰이지 않는다.**
`infra` 플레이북의 규칙으로 박는다:

- 사용자가 "안 돼요" / "모르겠어요"라고 하면 → **설명을 더 요구하지 말고 스크린샷을 요청**
- 안내한 화면과 실제 화면이 다를 가능성을 항상 열어둔다

두 번째가 중요한 이유: **AWS 콘솔 UI는 자주 바뀌고 우리 가이드는 낡는다.**
스크린샷 채널이 그 노후화에 대한 백스톱이다.

---

## 7. 실패를 전제로 — 탈출구

우리가 아무리 잘 만들어도 VPS 경로에서 일정 비율은 막힌다. **막혔을 때의 설계가 없으면
사용자는 그냥 사라진다.**

### 7.1 자동 감지와 선제 제안

프로듀서가 다음 신호를 감시한다:

- 같은 오류가 3회 반복
- 한 단계에서 30분 이상 진전 없음
- 사용자 발화에서 좌절 신호

감지되면 **사용자가 포기를 말하기 전에 먼저 제안한다:**

> 서버 설정이 잘 안 되네요. **지금까지 만든 건 하나도 안 없어집니다.**
> ① Railway로 먼저 띄워서 앱이 도는 걸 확인하기 (10분, 나중에 AWS로 다시 옮길 수 있어요) ← 추천
> ② 조금 더 시도해보기
> ③ 지금 화면을 보여주시면 제가 확인해볼게요

**"실패했으니 포기"가 아니라 "사다리에서 한 칸 내려간다"**가 되어야 한다.
§2에서 칸 사이 이동 비용을 낮게 설계한 이유가 여기서 회수된다.

### 7.2 내려갈 때 요금을 끊는다

사다리를 내려갈 때 EC2를 켜둔 채 잊으면 요금이 계속 나간다.
**탈출구에는 반드시 정리 절차가 포함된다.**

```
devoffice remote destroy
  → 콘솔에서 인스턴스·고정 IP·스냅샷·볼륨을 지우는 절차 안내
  → 특히 "고정 IP를 지우지 않으면 미사용 요금이 붙는다"는 함정을 명시
```

미사용 고정 IP 과금은 비개발자가 절대 예상 못 하는 대표적 함정이다.

### 7.3 어디서 막히는지 데이터를 남긴다

각 단계 진입·통과·이탈이 `journal.jsonl`에 기록된다. 전부 로컬 파일이므로
프라이버시 문제가 없고, 사용자가 이슈를 낼 때 그대로 첨부할 수 있다.

오픈소스 제품에서 **"어느 단계에서 사람들이 사라지는가"**는 가장 중요한 개선 신호다.
가이드를 감으로 고치지 않고 데이터로 고칠 수 있게 된다.

---

## 8. 결론 — 사용자가 실제로 손으로 하는 일 전부

우려에 대한 가장 직접적인 답은 목록이다. **VPS 경로에서도 사용자의 몫은 이만큼이다.**

### 기본 경로 (사다리 1~2칸) — v0.1

| # | 하는 일 | 소요 |
|---|---|---|
| 1 | GitHub 계정 생성 + `gh auth login` 승인 | 5분 |
| 2 | Vercel 계정 생성 (GitHub으로 로그인) | 2분 |
| 3 | (필요 시) Supabase 프로젝트 생성 | 5분 |

### VPS 경로 (사다리 4~5칸)

| # | 하는 일 | 자동화 불가 이유 |
|---|---|---|
| 1 | AWS 계정 생성 | 본인 인증 |
| 2 | 결제수단 등록 | 결제 |
| 3 | **예산 알림 설정** | 계정 권한 — 인스턴스 생성보다 **먼저** |
| 4 | 인스턴스 생성 (Lightsail 기준 클릭 ~5회) | 콘솔 |
| 5 | 키 파일 다운로드 후 지정 위치에 저장 | 브라우저 다운로드 |
| 6 | 방화벽 포트 열기 | 콘솔 |
| 7 | (도메인 사용 시) DNS 레코드 등록 | 도메인 등록업체 |

**그 외 전부 자동이다.** SSH 접속 이후의 서버 세팅, 배포, 로그 확인, 롤백,
SSL 발급·갱신, 재부팅 자동 시작 — 전부 명령 하나씩이다.

### 우려에 대한 최종 정리

| 우려 | 해소 방법 |
|---|---|
| ① EC2 구축이 어렵다 | 사다리로 대부분 4칸 이전에 붙잡음 · AWS 요구는 **Lightsail**로 유도 · SSH 이후 전부 `bootstrap` 1개 |
| ② 로컬 코드를 어떻게 올리나 | **rsync 직접 전송.** 서버는 GitHub을 모른다 |
| ③ GitHub 사용법을 가르쳐야 한다 | **가르치지 않는다.** 계정 생성 + `gh auth login` 승인 2개가 전부. GitHub은 금고, 배포는 rsync로 분리 |
| ④ 로컬 세션이 원격 원인을 못 찾는다 | `devoffice remote logs/status/snapshot` · 실패 시 **자동 스냅샷** · 코드와 증상을 **한 세션에서** 대조 |
| ⑤ EC2에 Claude를 열어야 한다 | 기본 아님(메모리·인증·컨텍스트 분리). 필요하면 `remote investigate`로 **원격을 서브에이전트처럼** 사용 |

**그럼에도 정직하게:** VPS 경로의 이탈률은 다른 경로보다 높을 것이다.
그래서 이 문서의 절반이 §2(사다리)와 §7(탈출구)에 할애되어 있다.
**성공률을 올리는 가장 확실한 방법은 어려운 경로를 잘 만드는 것이 아니라,
대부분의 사용자가 그 경로에 들어가지 않아도 되게 만드는 것이다.**
