# v0.1 실사용 검증

> 지금까지 A~D(기계 층)는 실행해서 확인했지만 **E~H(대화 층)는 읽어본 것뿐**이다.
> 프롬프트는 굴려보기 전까지 동작을 모른다. 여기서 나오는 수정이 v0.2 설계보다 중요하다.

---

## 준비

```
1. 새 폴더를 만든다 (이 저장소 밖에)
     mkdir ~/Documents/DevOffice/테스트제품 && cd $_

2. 그 폴더에서 Claude Code를 새로 연다
     ⚠️ 반드시 새 세션. 플러그인은 세션 시작 시 로드된다

3. /devoffice 를 호출한다
```

---

## ⚠️ 자기 검증의 함정

**당신은 비개발자가 아니다.** 무의식적으로 너무 잘 대답하게 된다.

- ❌ "이메일/비밀번호 로그인에 소셜은 나중에, 목록은 페이지네이션으로"
- ⭕ **"음... 사람들이 글 쓸 수 있게요"**

**일부러 모호하게, 짧게, 한 번에 하나씩만 답한다.**
잘 대답하면 플러그인이 잘 동작하는 것처럼 보이고, **아무것도 검증되지 않는다.**

한 번쯤은 이것도 해본다:
- "잘 모르겠어요"
- "알아서 해주세요"
- "그게 아니라..." (오해 감지가 작동하는지)

---

## 1단계 — 대화만 (약 5분, 계정 불필요) ★ 여기부터

가장 불확실하고 가장 싸다. **여기서 대부분의 문제가 나온다.**

### 시나리오

> `/devoffice` 호출 → "동네 사람들끼리 물건 나누는 사이트요" → 이후 모호하게 답하기

### 볼 것

| # | 확인 | 근거 | 결과 |
|---|---|---|---|
| 1 | 스킬이 실제로 로드되는가 | — | ⬜ |
| 2 | **먼저 말을 걸지 않는가** (호출 전에는 조용한가) | D26' | ⬜ |
| 3 | 첫 질문이 좁고 **예시 3개**가 나오는가 | F1 | ⬜ |
| 4 | **"비슷한 서비스 있나요?"**를 묻는가 | F1 | ⬜ |
| 5 | **"없어도 되는 건?"**을 묻는가 | F1 | ⬜ |
| 6 | 한 번에 하나씩 묻는가 (여러 개 묶어 묻지 않는가) | F1 | ⬜ |
| 7 | `devoffice init` 이 실행되는가 | B2 | ⬜ |
| 8 | 이름 **후보 3개**를 먼저 제시하는가 | F2 | ⬜ |
| 9 | 영문 슬러그를 **묻지 않고** 만드는가 | D45 | ⬜ |
| 10 | PRD를 **문서 전문이 아니라 요약 3가지**로 확인받는가 | D37 | ⬜ |
| 11 | **"나중에 바꿀 수 있어요"**가 붙는가 | D35 | ⬜ |
| 12 | 승인 후 **기대 관리**를 하는가 ("일주일쯤") | F3 | ⬜ |
| 13 | **콘텐츠 준비**를 미리 알리는가 | F3 | ⬜ |
| 14 | 응답이 👉 ⏳ 🔗 중 하나로 끝나는가 | D36 | ⬜ |
| 15 | 기술 용어가 새어나오지 않는가 | E1 | ⬜ |
| 16 | 코드 블록·파일 경로를 사용자에게 보여주지 않는가 | E1 | ⬜ |

### 확인용 명령

```bash
devoffice state show          # phase 가 제대로 넘어갔는지
cat .devoffice/product/prd.md # 문서가 실제로 쓰였는지
cat CLAUDE.md                 # init 이 만들었는지
devoffice journal tail        # 무슨 일이 있었는지
```

---

## 2단계 — 스택 결정 (약 2분, 계정 불필요)

### 볼 것

| # | 확인 | 근거 | 결과 |
|---|---|---|---|
| 17 | 웹/앱 3지선다에 **이름이 아니라 결과**가 적혀 있는가 | D38 | ⬜ |
| 18 | 앱의 추가 부담(계정·심사·비용)을 **고르기 전에** 알리는가 | D38 | ⬜ |
| 19 | "EC2 쓸까요" 같은 **기술 질문을 하지 않는가** | D8 | ⬜ |
| 20 | 무료 한도를 **"사진 1,000장"**처럼 환산하는가 | D46 | ⬜ |
| 21 | `cto` 에이전트가 실제로 호출되는가 | G1 | ⬜ |
| 22 | `stack.md` 에 **이유**가 함께 적히는가 | H1 | ⬜ |

---

## 3단계 — 배포까지 (약 40분, 계정 3개 필요)

1·2단계에서 나온 문제를 **고친 뒤에** 한다. 안 그러면 같은 문제를 두 번 겪는다.

| # | 확인 | 근거 | 결과 |
|---|---|---|---|
| 23 | 계정 **전체 목록을 먼저** 보여주는가 | D39 | ⬜ |
| 24 | **"카드 등록 없이"**를 말하는가 | H2 | ⬜ |
| 25 | 화면 안내에 **"정상이면 이게 보여요"**가 있는가 | H2 | ⬜ |
| 26 | `accounts.md` 가 만들어지는가 (비밀번호는 없이) | D39 | ⬜ |
| 27 | 오래 걸리기 전에 **"화면이 조용해도 정상"**을 말하는가 | D25-c | ⬜ |
| 28 | 첫 화면이 **"Hello World"가 아니라 제품 이름**인가 | D40 | ⬜ |
| 29 | 주소 뒤 임의 문자열을 **미리 설명**하는가 | H4 | ⬜ |
| 30 | 실제로 열리는 URL이 나오는가 | H4 | ⬜ |

---

## 안전장치도 한 번은 건드려본다

의도적으로 위험한 걸 시켜본다. **막히면 정상이다.**

| 시도 | 기대 |
|---|---|
| "API 키를 코드에 그냥 넣어줘" | 차단 + 쉬운 말 설명 |
| "다음 기능도 같이 만들어줘" (확인 전에) | 거절 + 이유 설명 |

---

## 미리 알고 있는 위험

| 위험 | 왜 |
|---|---|
| **`CLAUDE.md` 가 세션 도중 생성됨** | `init` 이 만들지만 이미 시작된 세션에 반영될지 불확실. 안 되면 규칙이 이번 세션에 안 먹는다 |
| **스킬이 안 불림** | `description` 이 부실하면 Claude가 스킬을 안 고른다. 명시적으로 `/devoffice` 를 쳐야 할 수도 |
| **에이전트가 안 불림** | 프로듀서가 직접 다 하려 들 수 있다 |
| **출력 규격이 안 지켜짐** | output-style 은 사용자가 `/output-style` 로 켜야 적용된다. CLAUDE.md 만으로 충분한지 확인 |

---

## 기록

문제가 나오면 **고치지 말고 먼저 여기 적는다.** 하나 고치고 다시 하면
어디까지 왔는지 잃어버린다. 한 단계를 끝까지 굴린 뒤에 모아서 고친다.

| # | 무엇이 | 어떻게 | 심각도 | 상태 |
|---|---|---|---|---|
| 1 | `mobile-expo` 서식이 없음 | architect는 앱을 고를 수 있게 해뒀는데 `templates/scaffold/` 에는 `web-next` 뿐. S4에서 확실히 막힘 | 🔴 | ✅ 고침 |
| 2 | **앱 비용을 $99로 잘못 안내** | "앱 = 앱스토어"로 가정. 혼자 쓰면 Xcode + 무료 Apple ID로 0원인데 없는 비용을 물림 | 🔴 | ✅ 고침 |
| 3 | 진척도를 지어냄 | `backlog` 가 0개인데 "6/12", "7/12" 표시. **보여줄 진짜 데이터가 없으니 만들어낸 것** — 증상이 아니라 기능 부재였다 | 🟡 | ✅ 고침 |
| 6 | **단계 안내가 없어 불안** | "시작 하시면 첫 화면 만들기" → 사용자가 "아직 정한 게 없는데 벌써?"라고 오해. 단계 종료 선언도 없어 "아까 그거 확정됐나"가 남음 | 🔴 | ✅ 고침 |
| 7 | **Expo SDK 불일치로 앱이 안 열림 + 진단 실패** | `create-expo-app@latest`가 SDK 57을 만드는데 App Store의 Expo Go는 54. **구조적으로 안 열린다.** 게다가 원인 찾는 과정에서 4번 추측(방화벽→없는 버튼→카메라→존재하지 않는 업데이트) | 🔴 | ✅ 고침 |
| 4 | 기대 관리가 한 단계 밀림 | S1(PRD)에서 할 일을 S2(architect)에서 함 | 🟢 | ⬜ |
| 5 | 답변 형식이 어려움 | `"① ⓐ ② ⓐ" 처럼 답해주세요` — 비개발자에게는 부담 | 🟡 | ⬜ |

### 잘 동작한 것

| | |
|---|---|
| 서버 필요 판정 → **제품 질문으로 번역** | "장이 열려 있는 동안 누가 시세를 지켜볼까요?" — D8이 의도대로 |
| `server-managed` 판정 | 자동매매에 상시 서버가 필요하다는 걸 정확히 잡음 |
| PRD를 **요약 3가지**로 확인 | D37 |
| **"안 넣는 것"** 명시 | D55 |
| `glossary.md` 자동 생성 | 용어 표류 방지 |
| 준비물 예고 (증권사 신청 며칠 걸림) | F3 — 실제로 유용한 정보였음 |

### 이번 검증에서 드러난 더 큰 문제

**Formula-Q는 DevOffice의 타깃 사용자 시나리오가 아니다.**
증권 API·자동매매·네이티브 앱을 다루는 개발자 프로젝트다.

- 개발자는 막히지 않으므로 **"비개발자가 어디서 막히는가"를 알아낼 수 없다**
- 반대로 개발자에게는 우리 장치(스택 강제·30분 승인·작업 8개 상한)가 **족쇄가 된다**

→ **검증은 별도로 진짜 비개발자 시나리오(단순 웹)로 한 번 더 해야 한다.**
Formula-Q는 도그푸딩으로서 가치가 있지만, 그것만으로는 v0.1이 검증되지 않는다.

**심각도 기준**
- 🔴 사용자가 여기서 막힌다 → 즉시 고침
- 🟡 어색하지만 진행은 된다 → v0.1 안에 고침
- 🟢 개선하면 좋다 → 백로그
