# DevOffice 안전망 설계

> 비개발자는 **묻지 않는다.** 몰라서 묻지 못하는 것들이 있고, 그중 일부는
> 사고가 나면 되돌릴 수 없다. 그래서 시스템이 대신 챙긴다.
>
> 앞선 문서들이 *"원하는 것을 만드는 법"*이라면, 이 문서는
> *"원하지 않는 일이 일어나지 않게 하는 법"*이다.

---

## 1. 왜 별도의 층이 필요한가

우리 설계의 모든 장치는 **사용자가 말한 것**을 기준으로 동작한다.

- 가정 명세 — 사용자가 말한 것의 빈칸을 채운다
- 4중 게이트 — 사용자가 승인한 명세대로 만들었는지 본다
- 그레이 영역 — 에이전트들이 서로 미룬 것을 잡는다

**이 전부가 "사용자가 요구할 수 있는 것"의 범위 안에 있다.**
비개발자가 애초에 존재를 모르는 것은 어느 장치에도 걸리지 않는다.

| 비개발자가 절대 말하지 않는 요구 | 없으면 |
|---|---|
| "남의 데이터를 못 보게 해주세요" | 아무나 남의 정보를 조회한다 |
| "개인정보처리방침을 만들어주세요" | **법 위반** |
| "DB를 백업해주세요" | 실수 한 번에 전부 사라진다 |
| "기존 기능이 안 깨지게 해주세요" | 조용히 깨진 채로 몇 주가 지난다 |
| "요금 폭탄이 안 나게 해주세요" | 청구서로 알게 된다 |

---

## 2. 위험 지도

| 위험 | 발생 확률 | 되돌릴 수 있나 | 대응 |
|---|---|---|---|
| **권한 미설정 (남의 데이터 노출)** | **높음** | ❌ 유출은 못 되돌린다 | §3 |
| **회귀 (기존 기능 파손)** | **매우 높음** | ⭕ 하지만 발견이 늦다 | §6 |
| **개인정보 법 위반** | 높음 | ⚠️ 과태료 | §4 |
| **데이터 손실** | 중간 | ❌ | §5 |
| **요금 사고** | 중간 | ⚠️ 청구 후 협의 | §7 |
| **비밀키 유출** | 중간 | ❌ | §3 |
| 모바일에서 깨짐 | 높음 | ⭕ | §8 |

위 두 개가 압도적으로 중요하다. **권한 미설정은 되돌릴 수 없고, 회귀는 반드시 일어난다.**

---

## 3. 보안 — 기본 방어선

### 3.1 비개발자 제품에서 실제로 터지는 것

이론적 취약점이 아니라 **실제로 자주 일어나는 것들**만 다룬다.

| 사고 | 어떻게 일어나나 |
|---|---|
| **RLS 미설정** | Supabase 테이블을 만들고 정책을 안 걸면 **누구나 전체 조회 가능** |
| **인증 없는 API** | `/api/users` 를 만들었는데 로그인 검사를 안 넣음 |
| **관리자 화면 노출** | `/admin` 을 만들고 권한 검사를 프론트에만 넣음 |
| **비밀키 커밋** | `.env` 를 실수로 올림 |
| **본인 확인 누락** | "내 글 수정"에서 작성자 확인을 안 함 |

**전부 "빠뜨려서" 생긴다. 잘못 만들어서가 아니다.**
그래서 체크리스트로 기계적으로 잡을 수 있다.

### 3.2 보안 게이트 — 4중 게이트에 하나를 더한다

[PROTOCOL §5](./PROTOCOL.md)의 게이트에 **L0 보안**을 추가한다.
번호를 0으로 두는 이유: **다른 게이트보다 먼저 통과해야 하고, 실패 시 무조건 차단**이다.

```
devoffice gate security
```

자동으로 검사 가능한 것:

- [ ] 새로 만든 테이블에 **RLS 정책이 있는가** (Supabase)
- [ ] 새로 만든 API 라우트에 **인증 검사가 있는가**
- [ ] 수정·삭제 엔드포인트에 **소유자 확인이 있는가**
- [ ] 관리자 경로가 **서버에서도** 보호되는가
- [ ] 소스에 **하드코딩된 키 문자열**이 없는가
- [ ] `.gitignore`에 `.env*`가 있는가

> **"프론트에서 버튼을 숨겼으니 됐다"가 비개발자 제품의 가장 흔한 착각이다.**
> 서버 검사가 없으면 안 한 것과 같다. 이건 그레이 영역([PROTOCOL §6](./PROTOCOL.md))의
> "클라이언트·서버 양쪽 검증" 항목과 같은 뿌리이며, 여기서는 **차단 게이트**로 격상된다.

### 3.3 사용자에게 보여주는 방식

보안을 기술 용어로 설명하면 사용자는 넘긴다. **결과로 말한다.**

> ❌ "comments 테이블에 RLS 정책이 없습니다"
>
> ✅ "지금 상태면 **다른 사람이 남의 댓글을 지울 수 있어요.** 막아둘게요." (자동 수정)

**보안은 질문하지 않는다. 고치고 통보한다.**
[DIALOGUE §5](./DIALOGUE.md)의 강도 2에 해당하며, 예산에서 제외된다.

---

## 4. 개인정보 — 한국 기준

### 4.1 비개발자가 모르는 사실

**이메일 하나만 받아도 개인정보 수집이다.** 회원가입이 있는 순간 법적 의무가 생긴다.

| 의무 | 실무 |
|---|---|
| 개인정보처리방침 게시 | 별도 페이지 + 하단 링크 |
| 수집·이용 동의 | 회원가입 시 체크박스 (선택/필수 구분) |
| 수집 항목 최소화 | 안 쓸 항목은 받지 않는다 |
| 만 14세 미만 | 법정대리인 동의 — 아니면 가입 차단 |
| 파기 | 탈퇴 시 삭제 절차 |

### 4.2 자동 생성 — 코드에서 수집 항목을 추출한다

개인정보처리방침을 사용자에게 쓰라고 하면 못 쓴다. 그런데 **우리는 무엇을 수집하는지
정확히 안다** — 데이터 모델과 계약(`packages/contracts/`)에 다 있다.

```
devoffice gate privacy
  → 스키마에서 개인정보성 필드 탐지 (email, name, phone, birth, address…)
  → 수집 항목·목적·보유기간을 채운 처리방침 초안 생성
  → 동의 UI가 회원가입 화면에 있는지 검사
  → 없으면 차단
```

**개인정보 필드가 추가되는 순간 자동으로 감지되어 방침이 갱신된다.**
사용자가 잊을 수 없는 구조다.

> **한계를 명시한다:** 자동 생성물은 초안이며 법률 자문이 아니다.
> 사용자에게 "실제 서비스 전에 한 번 확인이 필요합니다"라고 반드시 알린다.
> 우리가 법적 책임을 대신 질 수 있는 것처럼 말하지 않는다.

---

## 5. 데이터 — 백업과 복구

### 5.1 비개발자가 데이터를 잃는 전형적 경로

- 마이그레이션이 실패해서 테이블이 날아감
- "초기화해줘"라고 말했는데 진짜 초기화됨
- 서비스 무료 티어 만료로 프로젝트가 정지됨

### 5.2 대응

| 시점 | 장치 |
|---|---|
| **마이그레이션 직전** | **자동 스냅샷.** 예외 없음 |
| 매일 | 자동 export (무료 티어는 자동 백업이 없다) |
| 파괴적 명령 감지 시 | `PreToolUse` 훅으로 차단 후 확인 요구 |
| 복구 | `devoffice restore --to <시점>` |

**"실 데이터가 있는 상태"와 "없는 상태"를 시스템이 구분해야 한다.**
사용자가 실제로 쓰기 시작한 뒤로는 파괴적 작업의 문턱을 올린다.

```json
// state.json
"production": { "hasRealData": true, "firstUserAt": "2026-09-01" }
```

`hasRealData: true`가 되는 순간:
- 스키마 변경은 **되돌리기 비싼 것**으로 승격 → 반드시 확인 (DIALOGUE §5.3)
- 데이터 삭제를 동반하는 마이그레이션은 **강도 4**(대화)로

---

## 6. 회귀 — 이 문서에서 가장 중요한 부분

### 6.1 지금 설계의 구멍

4중 게이트는 **이번 기능만 본다.**
21번째 기능을 만들다가 3번 기능이 깨져도 **아무도 보지 않는다.**

비개발자는 자기가 안 건드린 화면을 다시 확인하지 않는다.
그래서 **몇 주 뒤에 발견하거나, 영영 발견하지 못한다.**

기능이 쌓일수록 확률이 올라가므로, **이건 가능성이 아니라 예정된 사고다.**

### 6.2 답: 사용자의 검증이 그대로 테스트가 된다

우리에게는 이미 완벽한 재료가 있다 — **`verify.md`**.
사용자가 직접 클릭해서 확인한 시나리오다.

> **사용자가 한 번 "됩니다"라고 말한 것은 다시는 깨지지 않는다.**

```
①  qa가 verify.md 작성        "로그아웃 상태로 글을 연다 → 로그인 버튼이 보인다"
②  사용자가 직접 확인          ✅
③  qa가 같은 시나리오를 자동화  Playwright 스크립트로 변환
④  이후 모든 기능에서 전체 실행  devoffice gate regression
⑤  깨지면 즉시 알림            "3번 기능(댓글)이 깨졌어요" → fixer
```

**사용자는 테스트의 존재를 알 필요가 없다.**
검증 시나리오를 쓰는 것도, 확인하는 것도 이미 하고 있던 일이다.
자동화는 그 부산물이다.

### 6.3 왜 이 방식이 옳은가

| 일반적인 접근 | 이 방식 |
|---|---|
| 개발자가 테스트를 따로 작성 | **검증 행위가 그대로 테스트가 된다** |
| 테스트가 실제 사용과 괴리 | **사용자가 실제로 한 행동 그대로** |
| 유지보수 부담 | 검증 시나리오를 고치면 테스트도 고쳐진다 |
| 비개발자가 이해 못 함 | **읽으면 이해된다** — 평문 시나리오 |

그리고 회귀 테스트가 깨졌을 때 사용자에게 이렇게 말할 수 있다:

> "새 기능을 넣다가 **예전에 확인하셨던 [댓글 삭제]가 안 되게 됐어요.**
> 고치고 다시 확인할게요. 1~2분 걸려요."

**사용자가 신뢰를 잃는 순간이 오히려 신뢰를 얻는 순간이 된다.**

### 6.4 정직한 한계

- **웹 우선.** Playwright는 웹에서 잘 되고, Expo/네이티브는 도구가 다르고 무겁다
  → 모바일은 v1에서 **수동 체크리스트 누적**으로 하고, 자동화는 이후
- 테스트 실행 시간이 기능 수에 비례해 늘어난다
  → 핵심 시나리오만 매번, 전체는 배포 전에
- 시각적 변화(디자인 깨짐)는 잡지 못한다
  → v1 범위 밖. 스크린샷 비교는 유지보수 비용이 크다

---

## 7. 요금 사고 방지

[DEPLOY.md](./DEPLOY.md)에서 EC2 요금을 다뤘지만, **코드가 만드는 요금**은 다루지 않았다.

| 사고 | 원인 | 대응 |
|---|---|---|
| API 호출 폭주 | 화면이 계속 새로고침 / 무한 루프 | 호출 횟수 상한 + 배포 전 검사 |
| 대역폭 초과 | 원본 이미지를 그대로 서빙 | **이미지 최적화 기본 적용** |
| DB 요금 | 인덱스 없이 전체 스캔 | 목록 조회에 인덱스·페이지네이션 기본 |
| 무료 한도 초과 | 사용자가 갑자기 늘어남 | 한도 80% 도달 시 알림 |

**전부 "기본값으로 넣으면 안 터지는 것"들이다.**
플레이북(`playbook-*.md`)에 상수로 박아 매번 새로 판단하지 않게 한다.

---

## 8. 그 외 기본 방어선

| 항목 | 왜 빠지나 | 대응 |
|---|---|---|
| **모바일 화면** | 만든 사람은 데스크탑에서만 본다 | 검증 시나리오에 **모바일 확인 단계 필수 포함** |
| **느린 네트워크** | 로컬은 항상 빠르다 | 로딩 상태 3종 세트 (이미 PROTOCOL §6.3) |
| **긴 텍스트·많은 데이터** | 테스트할 때 3개만 넣어본다 | 시드 데이터를 **의도적으로 많이·길게** |
| **에러 화면** | 정상 경로만 확인한다 | 에러 상태를 검증 시나리오에 포함 |
| **접근성** | 개념 자체를 모른다 | 컴포넌트 라이브러리(shadcn) 기본값에 위임 |

**세 번째가 의외로 자주 터진다.** 제목 3글자로 테스트하고 실제로 50글자가 들어오면
레이아웃이 무너진다. 시드 데이터를 현실적으로 만드는 것만으로 상당수가 예방된다.

---

## 9. 권한 — 에이전트가 내 컴퓨터에서 무엇을 하는가

비개발자가 느끼는 불안이고, 실제 위험이기도 하다.

**절대 권장하지 않을 것:** `--dangerously-skip-permissions`
비개발자가 승인 프롬프트에 지쳐 이걸 켜면 방어선이 통째로 사라진다.

**대신 제안하는 정책:**

| 범위 | 정책 |
|---|---|
| 제품 상자(프로젝트 폴더) 안 | 자유롭게 |
| 그 밖의 파일 | **차단** (`PreToolUse` 훅) |
| 네트워크 쓰기 (배포·푸시) | 확인 |
| 삭제·초기화 | 실 데이터가 있으면 **강도 4** |

승인 피로를 줄이는 방법은 권한을 푸는 것이 아니라 **묻는 횟수를 줄이는 것**이다 —
프로젝트 폴더 안에서의 작업을 한 번에 허용하면 대부분의 프롬프트가 사라진다.

플러그인이 권장 권한 설정을 **문서로 제공**하고, 첫 실행 시 안내한다.

---

## 10. 게이트 정리

이 문서가 추가하는 게이트를 기존 체계에 합치면:

| 게이트 | 시점 | 실패 시 |
|---|---|---|
| **L0 보안** | 구현 후, 다른 게이트보다 먼저 | **무조건 차단** + 자동 수정 시도 |
| L1 기계적 | 구현 후 | 차단 |
| L2 계약 | 구현 후 | 차단 |
| L3 증거 대조 | 통합 리뷰 | ❌ 있으면 차단 |
| L4 사용자 체험 | 검증 | 사용자 판단 |
| **L5 회귀** | 사용자 확인 통과 후, 출시 전 | 차단 → fixer |
| **개인정보** | 개인정보 필드 추가 시 | 차단 |

```
구현 → L0 보안 → L1 → L2 → L3 → L4 사용자 확인 → L5 회귀 → 출시
        ↑                              ↑              ↑
     되돌릴 수 없는 것          유일한 최종 권위    과거를 지킨다
```

---

## 11. 강제 장치

| 장치 | 방법 |
|---|---|
| L0·L5 미통과 시 출시 차단 | `devoffice gate` + `Stop` 훅 |
| 개인정보 필드 감지 → 방침 갱신 | 스키마 diff 검사 |
| 마이그레이션 전 자동 스냅샷 | `PreToolUse` 훅 |
| 실 데이터 존재 시 파괴적 작업 승격 | `state.json`의 `hasRealData` |
| 프로젝트 폴더 밖 쓰기 차단 | `PreToolUse` 훅 |
| 검증 통과 시 회귀 테스트 자동 생성 | qa 에이전트 규칙 + `gate regression` |

---

## 12. 로드맵 반영

| 버전 | 추가 |
|---|---|
| **v0.1** | 비밀키 차단, 프로젝트 폴더 밖 쓰기 차단 (이미 계획됨) |
| **v0.2** | **L0 보안 게이트** — 기능 루프와 동시에. 없이 기능을 만들면 부채가 쌓인다 |
| **v0.3** | **L5 회귀** — 기능이 5개를 넘기 전에 반드시 |
| v0.4 | 개인정보 게이트, 백업·복구 |
| v0.5 | 요금 방어, 권한 정책 문서화 |

> **L0과 L5의 순서가 중요하다.** 보안은 기능마다 쌓이는 부채라 처음부터 필요하고,
> 회귀는 기능이 몇 개 쌓인 뒤에야 의미가 생긴다. 다만 **필요해졌을 때 만들면 이미 늦다** —
> 그때는 과거 기능들의 검증 시나리오가 자동화되어 있지 않기 때문이다.
