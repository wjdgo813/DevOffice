# DevOffice 운영 프로토콜

> `DESIGN.md`가 **무엇을 만드는가**라면, 이 문서는 **조직이 어떻게 동작하는가**다.
> DevOffice의 실질적 산출물은 에이전트가 아니라 이 프로토콜이다.

---

## 1. 프로토콜이 핵심 자산인 이유

두 가지 플랫폼 사실이 이 문서 전체를 강제한다.

- **C1** 서브에이전트는 사용자에게 질문할 수 없다 (`AskUserQuestion` 제거됨)
- **C2** 서브에이전트는 메인 대화 이력을 받지 못한다. 자기 시스템 프롬프트 + 지시문 + CWD가 전부다

즉 **에이전트는 매번 기억상실 상태로 출근한다.** 어제 뭘 했는지, 옆자리가 뭘 만들었는지,
사용자가 뭘 원했는지 모른다. 조직이 굴러가려면 이 상태에서도 일이 이어지게 만드는
**문서 규약**이 있어야 한다.

이 문서는 5가지를 정의한다:

| § | 문제 | 장치 |
|---|---|---|
| 2 | 에이전트끼리 어떻게 인수인계하는가 | 작업 지시서 / 완료 보고서 |
| 3 | 세션이 끊겨도 어떻게 이어가는가 | 작업 단위 체크포인트 |
| 4 | 프로젝트마다 팀 구성이 다른 문제 | 정적 역할 + 동적 플레이북 |
| 5–6 | 누락과 그레이 영역을 어떻게 잡는가 | 4중 게이트 + 증거주의 |
| 7–8 | 막히거나 불가능한 걸 어떻게 알아채는가 | 에스컬레이션 / 타당성 검토 |
| **11** | **한 번에 너무 많이 만드는 것을 어떻게 막는가** | **작업 단위 상한 + 진행 잠금** |

> **§11은 다른 모든 장이 작동하기 위한 전제다.** 한 번에 많이 만들면
> 검증도 증거 대조도 그레이 영역 처리도 전부 성기어진다. 먼저 읽어도 좋다.

---

## 2. 인수인계 — 작업 지시서와 완료 보고서

### 2.1 두 가지를 혼동하지 않는다

- **인수인계(handoff)**: "일을 시작하려면 뭘 알아야 하는가" — 배턴 전달
- **진행 기록(worklog)**: "내가 어디까지 했는가" — §3에서 다룸

하나의 `handoff.md`에 둘을 섞으면 둘 다 못 쓴다. 분리한다.

### 2.2 파일 배치

```
.devoffice/features/F-003-comments/
├── spec.md                    사용자가 승인한 명세 (AC 번호 포함)
├── plan.md                    CTO 작성: 작업 분해 + 소유권
├── handoff/
│   ├── _baton.md              지금 배턴이 누구에게 있는가 + 순서
│   ├── 01-cto→backend.md      작업 지시서
│   ├── 02-cto→frontend.md     작업 지시서
│   ├── 03-backend→cto.md      완료 보고서
│   └── 04-frontend→cto.md     완료 보고서
├── worklog/
│   ├── backend.md             진행 체크포인트 (§3)
│   └── frontend.md
├── blockers/                  에스컬레이션 (§7)
└── verification.md            증거 대조표 (§5)
```

파일명에 순번을 붙이는 이유: **읽는 순서가 곧 사건의 순서**여야 한다. 재개하는 에이전트가
디렉토리만 봐도 흐름을 안다.

### 2.3 작업 지시서 규격

받는 에이전트는 **아무것도 모른다**는 전제로 쓴다. 7개 항목은 생략 불가다.

```markdown
# 작업 지시서 · F-003 · CTO → backend

## 1. 목적 (한 줄)
사용자가 글에 댓글을 달 수 있게 한다.

## 2. 먼저 읽을 파일  ← 경로를 명시한다. "관련 문서를 참고하라"는 금지
- .devoffice/features/F-003-comments/spec.md
- .devoffice/product/glossary.md
- .devoffice/product/playbook-backend.md
- packages/contracts/F-003.ts       ← 이 계약이 유일한 진실

## 3. 담당 작업
| ID | 내용 | 완료 조건 | 관련 AC |
|----|------|-----------|---------|
| T1 | POST /api/comments | 계약대로 201 + Comment 반환 | AC-1, AC-2 |
| T2 | Comment 테이블 마이그레이션 | 마이그레이션 실행 성공 | — |
| T3 | DELETE /api/comments/:id | 작성자만 성공, 타인은 403 | AC-3 |

## 4. 재사용할 것  ← CTO가 코드베이스를 조사해서 채운다. 비면 안 된다
- 인증 확인: lib/auth.ts 의 requireUser() 를 쓸 것. 새로 만들지 말 것
- 에러 응답: lib/api-error.ts 의 형식을 따를 것
- 조사 결과 재사용 가능한 것이 없으면 "없음 (조사함)" 이라고 적는다

## 5. 경로 권한
쓸 수 있음: supabase/**, apps/web/app/api/**
절대 금지: packages/contracts/**  ← 계약이 틀렸다면 고치지 말고 블로커를 발행할 것

## 6. 끝낼 때
- 작업마다: devoffice task done T1 --note "..."
- 전부 끝나면: handoff/03-backend→cto.md 작성 (§2.4 양식)

## 7. 막히면
추측해서 진행하지 말 것. devoffice task block T2 --type TECH_WALL 후 즉시 종료. (§7)
```

> **4번(재사용)이 "중복 구현" 문제의 실행 지점이다.** 에이전트는 기억이 없으므로
> 기존 코드를 알 수 없고, 모르면 새로 만든다. CTO가 계약 단계에서 조사해 지시서에 박아야
> 재사용이 실제로 일어난다. "재사용을 고려하라"는 프롬프트로는 아무 일도 일어나지 않는다.

### 2.4 완료 보고서 규격 — "하지 않은 것"이 핵심

에이전트는 **한 것만 보고하고 안 한 것은 침묵한다.** 이게 누락의 근원이다.
따라서 양식이 침묵을 허용하지 않아야 한다.

```markdown
# 완료 보고서 · F-003 · backend → CTO

## 완료
| ID | 결과 | 파일:라인 |
|----|------|-----------|
| T1 | ✅ | apps/web/app/api/comments/route.ts:12-48 |
| T2 | ✅ | supabase/migrations/0007_comments.sql |

## 완료하지 못한 것          ← 빈칸이면 "없음"이라고 명시. 생략 금지
| ID | 이유 | 남은 작업 |
|----|------|-----------|
| T3 | 403 처리 방식이 계약에 없음 | 블로커 B-001 발행함 |

## 내 담당이 아니라고 판단한 것   ★ 그레이 영역 센서 (§6)
- AC-2 (500자 제한): 서버에서 검증은 넣었으나, 입력창에서 막는 건 프론트 담당으로 봄
- 댓글 목록 페이지로 가는 링크: 어느 화면에 넣을지 명세에 없음

## 계약과 다르게 구현한 것      ← 있으면 안 되지만, 있으면 반드시 여기 적는다
- 없음

## 다음 사람이 알아야 할 것
- Comment.authorId 는 auth.users.id 를 참조. 조인 시 주의
```

**"내 담당이 아니라고 판단한 것"이 이 양식에서 가장 중요한 칸이다.**
각 에이전트가 여기에 적은 항목을 모으면 그게 곧 **그레이 영역 후보 목록**이고,
CTO는 통합 리뷰에서 이 목록을 **반드시 전부 처리**해야 한다 (§6).

---

## 3. 진행 기록 — 세션이 끊겨도 이어가기

### 3.1 추적 단위는 "에이전트"가 아니라 "작업"이다

"backend가 70% 했다"는 재개할 수 없다. "T1·T2 완료, T3 진행 중"은 재개할 수 있다.
따라서 `plan.md`는 반드시 **원자적이고 완료 조건이 있는 작업**으로 쪼개져야 한다.

### 3.2 기록은 "문서 작성"이 아니라 "명령 실행"으로

에이전트에게 "진행 상황을 문서에 기록하라"고 시키면 — 형식이 매번 달라지고, 잊어버리고,
작업이 끝난 뒤 한 번에 몰아 쓴다(그러면 중간에 죽었을 때 아무것도 안 남는다).

→ **한 줄 명령으로 만든다.** 플러그인의 `bin/devoffice` 실행 파일이 담당한다.

```bash
devoffice task start T3
devoffice task done  T3 --note "route.ts:12 에 구현"
devoffice task block T4 --type TECH_WALL --note "..."
```

이 명령 하나가 동시에 세 곳을 갱신한다:

```
worklog/backend.md    사람이 읽는 진행 기록
state.json            현재 위치 (SessionStart 훅이 읽음)
journal.jsonl         이벤트 로그 (Phase 2 GUI 연료)
```

**작업이 끝날 때가 아니라 각 작업이 끝날 때마다 즉시 실행**하는 것이 규칙이다.
스크립트가 형식을 보장하므로 문서가 깨지지 않고, 결정론적이라 LLM 토큰도 들지 않는다.

### 3.3 세션 재개 흐름

```
사용자가 창을 닫음
        ↓
다시 열림 → SessionStart 훅 → state.json 읽어 컨텍스트 주입
        ↓
프로듀서: "지난번에 [댓글 기능]을 만들다가 [서버 작업 3개 중 2개]까지 했어요.
           이어서 할까요?"
        ↓
재개 시 → 담당 에이전트에게 worklog + 원래 지시서를 함께 전달
          (새 지시서를 쓰지 않는다. 원본 + 진행 기록 = 재개 컨텍스트)
```

**중단된 작업(`in_progress`)은 재개 시 항상 처음부터 다시 한다.** 절반 쓰인 파일을
이어 쓰려 하면 더 위험하다. 작업을 원자적으로 쪼개는 이유가 여기 있다 —
쪼개져 있으면 다시 해도 싸다.

---

## 4. 동적 구성 — 프로젝트마다 팀이 달라진다

### 4.1 문제

플러그인 에이전트는 **정적 마크다운 파일**이다. 그런데 필요한 팀은 프로젝트마다 다르다:

- 웹 / 앱 / 둘 다 → 프론트엔드가 하는 일이 다름
- 서버 불필요 (BaaS) / 매니지드 서버 / EC2 직접 운영 → 백엔드 역할이 근본적으로 다름
- EC2를 쓴다면 → 인프라 담당이 **추가로 필요**

### 4.2 해결: 정적 역할 + 동적 플레이북

에이전트 파일은 **고정**한다. 대신 그 안에 강제 규칙을 넣는다:

> 작업 시작 전 반드시 `.devoffice/product/playbook-<역할>.md` 를 읽는다.
> 플레이북과 이 지시문이 충돌하면 **플레이북이 이긴다**.

그리고 S2(설계)에서 CTO가 사용자의 선택에 맞춰 플레이북을 **생성**한다.
플러그인은 `templates/playbooks/` 에 프리셋별 원본을 싣고, CTO가 골라 조합·조정한다.

이 방식의 이점:

- 에이전트 파일을 런타임에 만들지 않는다 (새 `agents/` 디렉토리 생성 시 재시작이 필요한 문제 회피)
- 플레이북이 `.devoffice/`에 있으니 **사용자도 볼 수 있고, git에 남고, 세션을 넘어 지속**된다
- Phase 2 GUI에서 "우리 팀 규칙" 화면으로 그대로 렌더링된다
- 오픈소스 기여자가 **플레이북만 추가해도** 새 스택이 지원된다

### 4.3 로스터 활성화

`state.json`이 이번 프로젝트에서 실제로 쓰는 에이전트를 들고 있다.

```json
{
  "roster": {
    "cto": "always", "research": "always", "qa": "always", "fixer": "always",
    "frontend": ["apps/web", "apps/mobile"],
    "backend": "active",
    "infra": "inactive"
  }
}
```

프로듀서는 이 목록만 호출한다. 비활성 에이전트는 존재하지만 부르지 않는다.

### 4.4 백엔드 형태는 사용자에게 묻지 않고 CTO가 판정한다

"EC2를 쓸까요, Firebase를 쓸까요?"는 비개발자에게 던지면 안 되는 질문이다(원칙 P5).
대신 **CTO가 PRD를 읽고 판정**한다.

| PRD에서 이런 신호가 보이면 | 필요한 것 |
|---|---|
| 로그인·회원 | 인증 (BaaS로 충분) |
| 데이터 저장·공유, 파일 업로드 | DB·스토리지 (BaaS로 충분) |
| 채팅·알림 등 실시간 | realtime (BaaS로 충분) |
| 결제 | 서버 로직 (BaaS 함수로 대개 충분) |
| **정기 실행·크롤링·배치·무거운 연산** | **상시 서버 필요** |
| **사내망·특수 포트·전용 소프트웨어** | **VPS 필요** |

**백엔드 프리셋 4단계 — 오른쪽으로 갈수록 사용자 부담이 커진다:**

| 프리셋 | 대상 | 사용자 부담 |
|---|---|---|
| `baas-supabase` (기본) | 대부분의 케이스 | 계정 생성만 |
| `baas-firebase` | 실시간·모바일 푸시가 핵심 | 계정 생성만 |
| `server-managed` (Railway/Render/Fly) | 상시 서버는 필요하나 서버 관리는 하기 싫다 | 계정 + 카드 |
| `server-lightsail` | "AWS를 쓰고 싶다" | AWS 계정 + 콘솔 클릭 ~5회 |
| `server-vps` (AWS EC2 등) | 사내망·특수 요구·완전한 제어 | AWS 계정 + 콘솔 클릭 ~10회 |

> **`server-managed`라는 중간 옵션이 결정적이다.** "상시 서버가 필요하다" ≠ "EC2가 필요하다".
> 그리고 "AWS를 쓰고 싶다" ≠ "EC2가 필요하다" — 그건 **Lightsail**로 답한다(고정 요금).
> **EC2는 정말 필요할 때만 열리는 마지막 옵션**이어야 한다.
>
> 이 다섯 단계는 **사다리**이며 칸 사이 이동 비용이 낮게 설계되어 있다.
> 그래서 "먼저 아래 칸으로 띄워보고 부족하면 옮긴다"가 항상 가능하다 → [DEPLOY §2](./DEPLOY.md)

판정 결과는 **기술 선택이 아니라 제품 트레이드오프로 번역해서** 보고한다:

> "만드시려는 기능 중 [매일 새벽 자동 수집]이 있어서, 항상 켜져 있는 서버가 필요합니다.
> ① 서버를 두고 그 기능을 넣는다 (설정 30분 추가, 월 ○원)
> ② 그 기능만 빼고 나머지를 먼저 만든다 (설정이 훨씬 간단, 나중에 추가 가능)
> 어느 쪽으로 할까요?"

### 4.5 `server-vps`를 선택했을 때 — `infra` 에이전트

EC2 직접 운영은 **비개발자에게 질적으로 다른 난이도**다. 전용 에이전트 + 전용 가이드가 붙는다.

**요금 안전장치를 최우선으로 배치한다.**
EC2를 켜놓고 잊어버려 청구서를 맞는 것은 이 제품이 사용자에게 입힐 수 있는 **가장 큰 실질적 피해**다.
따라서 인스턴스 생성 **이전에** 다음이 완료되어야 한다:

1. 프리티어 조건과 **초과 시점** 명시 (12개월 후 / 750시간 초과 / 트래픽)
2. AWS Budgets 예산 알림 설정 (금액 임계치 + 이메일)
3. 인스턴스 타입별 월 예상 비용을 원화로 안내
4. **"안 쓸 때 끄는 법"과 "완전히 지우는 법"**을 처음부터 알려준다

그 다음 순서:

| 단계 | 비개발자가 실제로 막히는 지점 |
|---|---|
| 계정·결제수단 | 해외 결제 카드, 본인 확인 |
| 리전 선택 | 서울(ap-northeast-2) 고정 안내 |
| 키페어(.pem) | **분실하면 접속 불가.** 저장 위치를 지정해주고 백업을 강제 |
| 보안 그룹 | 22/80/443만. `0.0.0.0/0` 전체 개방 경고 |
| SSH 접속 | 터미널 공포 구간. 접속 명령을 복사 가능한 형태로 |
| 도메인·DNS | 구입처별 네임서버 설정이 다름 |
| SSL | Let's Encrypt 자동화 + 갱신 |
| 프로세스 관리 | **재부팅 후 자동 시작**이 빠지면 조용히 죽는다 |
| 운영 | 로그 위치, 디스크 참, 백업 |

`infra` 에이전트의 역할은 **자동화 경계선에서 갈린다:**

- **콘솔 구간**(계정·결제·예산알림·인스턴스 생성·키 파일·방화벽) → 절차서를 생성한다.
  안내는 반드시 4요소(어느 화면 / 어디를 보고 / 무엇을 하고 / **정상이면 무엇이 보이는지**)를 갖춘다
- **SSH 접속 이후 전 구간** → 절차서가 아니라 **`devoffice remote bootstrap` 명령 1개**로 처리한다.
  사람이 20단계 가이드를 따라 하면 반드시 틀리기 때문이다

사용자가 "안 돼요"라고 하면 **설명을 더 요구하지 말고 스크린샷을 요청한다.**
비개발자는 화면 상태를 정확히 설명하지 못하고, AWS 콘솔 UI는 자주 바뀌어 우리 가이드는 낡는다.

전체 설계 → **[DEPLOY.md](./DEPLOY.md)** (§4 자동화 경계선, §6 콘솔 안내 규격, §7 탈출구)

---

## 5. 검증 — 4중 게이트와 증거주의

> ⚠️ **이 장 전체의 전제:** 4중 게이트는 *"명세대로 만들었는가"*만 답한다.
> *"명세가 맞는가"*는 [DIALOGUE.md](./DIALOGUE.md)만 답할 수 있다.
> 명세가 틀렸으면 이 게이트들은 **틀린 것을 100% 통과시킨다.**

### 5.1 전제: 명세가 체크 가능해야 검증이 가능하다

`spec.md`의 인수 조건은 반드시 **번호가 붙은 검증 가능한 문장**이어야 한다.
"댓글 기능이 잘 동작한다"는 검증할 수 없다.

```
AC-1  로그인한 사용자만 댓글을 쓸 수 있다
AC-2  댓글은 500자를 넘을 수 없다
AC-3  자기가 쓴 댓글만 지울 수 있다
AC-4  댓글이 하나도 없으면 "첫 댓글을 남겨보세요"가 보인다
AC-5  글 상세 화면에서 댓글 영역으로 갈 수 있다
```

AC를 쓰는 것은 프로듀서이고, 사용자는 O/X만 하면 된다 (원칙 P5).

### 5.2 4중 게이트

| 층 | 무엇을 잡는가 | 누가 | 비용 | 신뢰도 |
|---|---|---|---|---|
| **L1 기계적** | 타입 오류·빌드 실패·린트 | `devoffice gate l1` | 무료 | 100% |
| **L2 계약** | 엔드포인트가 계약대로 실제 응답하는가 | `devoffice gate l2` | 싸다 | 높음 |
| **L3 대조** | AC 하나하나에 대한 **코드 근거** | cto (읽기) | 중간 | 중간 |
| **L4 체험** | 실제 화면에서 되는가 | qa → 사용자 | 비쌈 | **최종 권위** |

**L2가 계약 설계(원칙 P3)의 배당금이다.** `packages/contracts/`에 zod 스키마가 있으므로,
각 엔드포인트에 요청을 보내고 응답이 스키마를 통과하는지 자동 검사할 수 있다.
사용자에게는 "서버가 약속한 대로 답하는지 확인했습니다"로 번역된다.

### 5.3 L3 — 증거주의 대조표

**이게 "누락"을 잡는 핵심 장치다.** CTO는 "잘 된 것 같다"라고 쓸 수 없고,
**AC마다 파일:라인 근거를 대야 한다.**

```markdown
# 검증 대조표 · F-003

| AC | 판정 | 근거 |
|----|------|------|
| AC-1 | ✅ | apps/web/app/api/comments/route.ts:12 (requireUser) |
| AC-2 | ⚠️ | route.ts:24 서버 검증만 있음. 입력창에서 막지 않아 UX 나쁨 |
| AC-3 | ❌ | 서버는 route.ts:61 에 있으나 프론트에서 삭제 버튼이 **모두에게** 보임 |
| AC-4 | ✅ | components/CommentList.tsx:30 |
| AC-5 | ❌ | 근거 없음 — 댓글 영역으로 가는 진입점이 어디에도 없음 |
```

**규칙: 근거를 대지 못하면 미구현으로 간주한다.**
에이전트의 "다 했습니다"를 믿지 않고 증거를 요구하는 구조다.

**❌가 하나라도 있으면 L4(사용자 검증)로 넘어갈 수 없다.** 게이트다.
❌는 `fixer`에게 넘어가고, 대조표는 수정 후 다시 채워진다.

### 5.4 L4 — 사용자 검증

`verify.md`는 사용자가 그대로 따라 할 수 있는 클릭 시나리오다. AC와 1:1 대응한다.

```markdown
1. 로그아웃 상태로 아무 글이나 열어보세요
   → 댓글 입력창 대신 "로그인하고 댓글 달기"가 보이면 정상입니다  (AC-1)
2. 로그인하고 같은 글을 열어 댓글을 써보세요
   → 댓글이 바로 목록에 나타나면 정상입니다
3. 아주 긴 글(500자 이상)을 붙여넣어 보세요
   → 더 이상 입력이 안 되면 정상입니다  (AC-2)
```

가능하면 `qa`가 스크린샷을 함께 남긴다 — 사용자가 확인하기 전에 이미 증거가 있다.

---

## 6. 그레이 영역 — 아무도 만들지 않은 것

에이전트를 나누면 **경계의 일이 사라진다.** 백엔드는 "프론트가 하겠지", 프론트는
"백엔드가 하겠지" 하고 둘 다 안 한다. 3중으로 막는다.

### 6.1 계획 단계 — AC×작업 매트릭스

CTO가 `plan.md`를 낼 때, **모든 AC가 최소 하나의 작업에 매핑**되어야 한다.
매핑되지 않은 AC가 있으면 계획이 미완성이다.

```
        T1  T2  T3  T4  T5
AC-1    ●               ●
AC-2    ●           ●
AC-3            ●       ●
AC-4                ●
AC-5    ← 매핑 없음! 계획 반려
```

`devoffice gate plan`이 이 매트릭스를 기계적으로 검사한다.

### 6.2 실행 단계 — "내 일이 아닌 것 같다" 신고

§2.4의 완료 보고서에 있는 그 칸이다. 각 에이전트가 지목한 항목이 모여
**그레이 영역 후보 목록**이 되고, CTO는 통합 리뷰에서 **하나도 남김없이** 처리해야 한다.
각 항목의 결말은 셋 중 하나여야 한다:

- 다른 에이전트에게 추가 작업으로 배정
- 이번 기능 범위가 아님으로 판정 + **근거 기록**
- 명세 공백으로 판정 → 블로커 발행 (§7)

### 6.3 상수 체크리스트 — 매번 새로 생각하지 않는다

경험적으로 항상 빠지는 것들을 CTO 플레이북에 상수로 박아둔다.

- [ ] **로딩 / 빈 상태 / 에러 상태** 3종 세트
- [ ] 권한 없는 사용자가 접근했을 때
- [ ] 입력 검증 — **클라이언트와 서버 양쪽** (한쪽만 하면 그레이 영역)
- [ ] DB 마이그레이션을 실제로 실행했는가
- [ ] 환경변수를 추가했다면 `.env.example` 갱신
- [ ] **기존 화면에서 이 기능으로 들어가는 경로** ← 가장 흔한 누락

> 마지막 항목이 압도적으로 자주 발생한다. "댓글 기능을 만들었는데 댓글로 가는 버튼이 없다."
> 그래서 **모든 기능의 마지막 작업은 항상 "진입 경로 연결"로 고정한다.**

---

## 7. 에스컬레이션 — 막히면 멈춘다

### 7.1 규칙: 추측해서 진행하지 않는다

일반적인 코딩 에이전트는 애매하면 합리적으로 추측하고 진행하는 게 맞다.
**이 제품에서는 아니다.** 비개발자는 잘못된 추측을 알아채지 못하고,
나중에 발견하면 되돌리는 비용이 훨씬 크다.

> **막히면 멈추는 것이 막히면 알아서 하는 것보다 항상 낫다.**

에이전트는 C1 때문에 사용자에게 직접 물을 수 없다. 대신 **구조화된 블로커를 남기고 종료**한다.

### 7.2 블로커 규격

```markdown
# B-001 · F-003 · backend
type: SPEC_UNCLEAR | TECH_WALL | MISSING_DEPENDENCY | SCOPE_TOO_BIG
raisedBy: backend
blocks: T3

## 무엇이 막혔는가 (사실만)
남의 댓글을 지우려 할 때 어떻게 응답할지가 명세와 계약에 없다.

## 왜 막혔는가
AC-3은 "자기 댓글만 지울 수 있다"이지만, 남의 댓글에 대해
① 삭제 버튼을 숨길지 ② 눌러도 막을지 ③ 둘 다인지가 정해지지 않았다.

## 선택지
A. 버튼을 숨긴다 — 간단, 30분, 다만 API 직접 호출은 여전히 가능
B. 버튼도 숨기고 서버에서도 막는다 — 안전, 1시간  ← 추천
C. 지금은 넘어가고 나중에

## 추천
B. 보안 관련이라 서버 차단이 없으면 나중에 문제가 된다.
```

### 7.3 프로듀서의 대응

블로커를 받으면 프로듀서가 **사용자 언어로 번역**해서 객관식으로 제시한다(`AskUserQuestion`).
타입별 기본 대응:

| 타입 | 프로듀서의 행동 |
|---|---|
| `SPEC_UNCLEAR` | 명세에 **없는** 것. 객관식 질문 → `spec.md` 갱신 → 같은 지시서로 재개 |
| **`SPEC_WRONG`** | 명세가 **틀린** 것. **부분 수정 금지** — 명세를 폐기하고 재작성 → [DIALOGUE §7](./DIALOGUE.md) |
| `TECH_WALL` | 대안 제시 (기능 축소 / 다른 방식 / 보류) — 필요하면 `research` 호출 |
| `MISSING_DEPENDENCY` | 계정·키·설치 안내 (`env-doctor` 호출) → 준비되면 재개 |
| `SCOPE_TOO_BIG` | 기능을 쪼개 백로그에 재편성. **현재 기능은 작은 조각으로 완결** |

> `SPEC_WRONG`은 구현 에이전트도 발행한다 — "명세대로 만들면 말이 안 되는 결과가 나온다"를
> 발견하는 건 대개 구현 중이기 때문이다.

사용자에게는 이렇게 보인다:

> 댓글 삭제를 만들다가 하나 확인이 필요해요.
> **남이 쓴 댓글에는 삭제 버튼을 어떻게 할까요?**
> ① 아예 안 보이게 (간단)
> ② 안 보이게 + 몰래 시도해도 막기 (더 안전, 30분 더) ← 추천
> ③ 지금은 넘어가기

### 7.4 중단 지점은 반드시 기록된다

블로커 발행은 `devoffice task block`을 통해서만 한다. 그래야 `worklog`·`state.json`·
`journal.jsonl`이 함께 갱신되고, 사용자가 며칠 뒤 돌아와도 정확히 그 지점에서 재개된다.

---

## 8. 타당성 검토 — 만들 수 있는지 먼저 확인한다

### 8.1 문제

사용자가 "서울 부동산 호가 리스트"를 원한다 → 백로그에 넣고 확정 → 2주 뒤 구현하려니
**호가 데이터를 주는 API가 없다**는 걸 그때 알게 된다. 여기까지 쓴 시간이 전부 낭비다.

> **타당성 검토는 백로그 확정 시점이 아니라 기능이 언급되는 즉시 시작되어야 한다.**

그렇다고 모든 기능을 깊이 조사하면 너무 비싸다. **2단계 필터**를 쓴다.

### 8.2 1단계 — 즉시 3색 분류 (싸다)

CTO가 PRD·백로그를 쓰는 중에 모든 기능을 즉시 색칠한다. 조사 없이 판단만 한다.

| 색 | 기준 | 예 | 조치 |
|---|---|---|---|
| 🟢 | 표준 기능. 외부 의존 없음 | 로그인, 글쓰기, 목록, 검색, 좋아요 | 조사 불필요 |
| 🟡 | 외부 서비스 의존 | 결제, 지도, 소셜 로그인, 이메일 발송, 푸시 | 가벼운 확인 |
| 🔴 | 데이터·법적·기술적 불확실 | **특정 데이터 확보**, 크롤링, AI 모델, 실시간 대용량 | **깊은 조사 필수** |

### 8.3 2단계 — 🟡🔴만 `research` 에이전트에게 (비싸다)

**백로그를 사용자에게 승인받기 전에** 일괄 조사한다. 조사 비용이 실제 위험이 있는 곳에만 든다.

### 8.4 `research` 에이전트 — 겉핥기를 구조적으로 막는다

LLM은 "네이버 부동산 API가 있습니다"라고 그럴듯하게 답한다. 실제로는 없거나, 있어도
사업자 등록이 필요하거나, 유료거나, **이용약관상 금지**다.

→ **조사 결과를 "의견"이 아니라 "증거"로 강제한다.**

```markdown
# R-003 · 서울 부동산 호가 정보

## 결론
구현 가능성: 조건부 가능
신뢰도: 높음 (실제 호출로 확인)
조사일: 2026-08-10   유효기간: 2026-09-09

## 확인한 것 — 근거 없는 주장은 결론에 쓸 수 없다
| 항목 | 확인 방법 | 근거 | 결과 |
|------|-----------|------|------|
| 공공데이터포털 실거래가 API | 실제 호출 | https://... | ✅ 응답 확인 |
| 호가(실거래가 아님) 제공 | 문서 확인 | https://... | ❌ 없음 |
| 민간 사이트 크롤링 | 이용약관 확인 | https://.../terms 제7조 | ❌ 금지 |

## 실제로 막히는 지점
- 원하시는 "호가"는 공공 데이터에 존재하지 않는다. 실거래가만 있다
- 민간 데이터는 약관상 수집이 금지되어 있다

## 사용자가 준비해야 할 것 (실거래가로 대체할 경우)
- 공공데이터포털 가입 + API 키 신청 (승인 1~2일 소요)
- 무료. 일 1000회 제한

## 대안
A. 실거래가로 대체 — 즉시 가능, 원래 가치의 약 80%   ← 추천
B. 유료 데이터 구매 — 월 ○○원, 계약 필요
C. 이 기능 보류
```

**강제 규칙 5가지:**

1. **근거 URL이 없는 주장은 결론에 쓸 수 없다**
2. "~인 것 같다" 금지. 확인했으면 확인, 못 했으면 **"확인 불가"라고 쓴다**
3. **가능하면 실제로 호출해본다** (`curl`). 문서만 읽었으면 신뢰도를 "중간"으로 낮춘다
4. **요금·인증 요구사항·이용약관 제한을 반드시 확인한다** — 기술적으로 가능해도 법적으로 불가한 경우가 많다. 크롤링 케이스는 대부분 여기서 걸린다
5. **불가능하면 반드시 대안을 낸다.** "안 됩니다"로 끝내지 않는다

`research` 도구: `WebSearch`, `WebFetch`, `Bash`(실호출), `Read` / 쓰기는 `.devoffice/research/`만.

> **"실제로 호출해본다"가 이 에이전트를 일반 검색과 구별하는 지점이다.**
> 문서 읽기 = 중간 신뢰도, 실호출 성공 = 높은 신뢰도. 신뢰도를 명시하면
> 사용자가 위험을 스스로 판단할 수 있다.

### 8.5 재검토 트리거

한 번 🟢였다고 영원히 🟢가 아니다.

- 구현 직전(계약 단계)에 CTO가 "외부 의존이 생겼는가" 재확인
- 조사 결과에 **유효기간 30일**. 지나면 재확인 — 외부 API는 바뀐다
- 사용자가 검증 단계에서 새 기능을 말하면 → 백로그 편입 시 **즉시 3색 분류**

---

## 9. 프로토콜을 강제하는 방법

프롬프트에 쓴 규칙은 언젠가 어겨진다. 강제되는 것만 지켜진다.

### 9.1 `bin/devoffice` — 결정론이 사는 곳

```
devoffice state show                     현재 상태
devoffice task start|done|block <ID>     진행 기록 (worklog+state+journal 동시 갱신)
devoffice handoff new <from> <to>        지시서 템플릿 생성
devoffice gate plan                      AC×작업 매트릭스 검사 (§6.1)
devoffice gate l1                        타입·빌드·린트
devoffice gate l2                        계약 스모크 테스트
devoffice doctor                         환경 점검 (멱등)
```

**LLM은 판단하고, 스크립트는 상태를 다룬다.** 이 경계를 지키면 형식이 깨지지 않고
토큰도 아낀다.

### 9.2 훅

| 이벤트 | 강제 내용 |
|---|---|
| `SessionStart` | `state.json` 주입 → 이어하기 (§3.3) |
| `SubagentStop` | 완료 보고서 없이 끝나면 경고 |
| `Stop` | ❌ 남은 대조표를 두고 다음 기능으로 넘어가려 하면 차단 (§5.3) |
| `PreToolUse(Bash)` | 비밀키 커밋 차단, 소유 밖 경로 쓰기 차단 |
| `PostToolUseFailure` | 에러 → 쉬운 말 번역 |

### 9.3 에이전트 프롬프트의 4대 불변 규칙

모든 에이전트 지시문 최상단에 동일하게 들어간다.

1. **시작 전에 지시서의 "먼저 읽을 파일"을 전부 읽는다.** 추측으로 시작하지 않는다
2. **작업 하나가 끝날 때마다 즉시** `devoffice task done` 을 실행한다. 몰아서 하지 않는다
3. **막히면 추측하지 않고 블로커를 남기고 종료한다** (§7.1)
4. **완료 보고서에서 "하지 않은 것"과 "내 담당이 아닌 것 같은 것"을 반드시 채운다.**
   빈칸이면 "없음"이라고 명시한다 (§2.4)

---

## 10. 이 프로토콜이 로스터에 미치는 영향

| 에이전트 | 상태 | 비고 |
|---|---|---|
| cto | 상시 | 계약·계획·L3 대조·그레이 영역 처리 |
| **research** | 상시 | **신규 (§8)** |
| backend | 조건부 | BaaS면 역할 축소, 서버면 확대 |
| frontend | 조건부 (1~2회 호출) | 웹/앱에 따라 |
| **infra** | 조건부 | **신규. `server-vps`에서만 (§4.5)** |
| qa | 상시 | 읽기 전용 |
| fixer | 상시 | L3 ❌ 항목 수리 |
| env-doctor | 상시 | |
| ~~spec-writer~~ | **보류** | 프로듀서가 직접 쓰고, 컨텍스트 압박이 실제로 확인되면 분리 |

정의된 역할은 8개지만 **한 프로젝트에서 동시에 활성인 것은 보통 5~6개**다.
로스터를 동적으로 관리하는 것(§4.3)이 "에이전트를 늘리지 않는다"는 원칙과
"프로젝트마다 팀이 달라야 한다"는 요구를 동시에 만족시키는 방법이다.

---

## 11. 한 번에 하나씩 — 기계적 강제

### 11.1 왜 이게 다른 모든 장의 전제인가

한 번에 많이 만들면 **사용자와 에이전트가 동시에 누락한다.**

- 사용자는 검증할 게 많아지면 대충 훑고 "네"라고 한다
- 에이전트는 작업이 커지면 중간을 빠뜨리고도 "완료"라고 한다
- 증거 대조(§5.3)도 그레이 영역 수집(§6.2)도 **대상이 많아질수록 성겨진다**

**"한 번에 하나씩"은 사용자 배려가 아니라 품질 장치다.**
그런데 지금까지의 설계는 이걸 *의도*로만 두고 **기계적으로 강제하지 않았다.**

### 11.2 세 가지 상한 — 기능이 크면 쪼갠다

"기능 하나씩"만으로는 부족하다. **기능 하나가 너무 클 수 있다.**

| 상한 | 값 | 넘으면 |
|---|---|---|
| 인수 조건(AC) | **5개 이하** | 쪼갠다 |
| 작업(T) | **8개 이하** | 쪼갠다 |
| 예상 시간 | **30분 이하** | 쪼갠다 |

`devoffice gate spec` / `gate plan`이 기계적으로 검사한다.
넘으면 CTO가 **자동으로 쪼개서 백로그에 재편성**하고 사용자에게 보고한다:

> "생각해보니 이 기능이 좀 커요. 둘로 나눠서 하나씩 확인하시는 게 나을 것 같아요.
>  ① 물건 목록 보기 (20분) ② 물건 상세 화면 (15분)
>  이렇게 나눠도 괜찮을까요?"

**쪼개는 것을 사용자에게 알린다.** 몰래 쪼개면 진척이 느려 보인다.

### 11.3 진행 잠금 — 검증 전에는 다음이 시작되지 않는다

지금까지는 `Stop` 훅에서 **경고**만 하기로 했다. **차단으로 격상한다.**

```json
// state.json
"currentFeature": {
  "id": "F-003",
  "gates": { "specApproved": true, "userVerified": false }
}
```

> **`userVerified: false`인 기능이 있으면 다른 기능의 작업을 시작할 수 없다.**

`devoffice task start`가 실패한다. 프로듀서가 우회하려 해도 스크립트가 막는다.
사용자가 "다음 것도 같이 해주세요"라고 해도 마찬가지다:

> "먼저 지금 만든 걸 한 번만 봐주세요. 확인되면 바로 다음 걸 시작할게요.
>  (한꺼번에 만들면 나중에 어디가 잘못됐는지 찾기 어려워져요)"

**예외 없음.** 이 잠금이 풀리는 유일한 방법은 사용자의 확인이다.

### 11.4 무엇을 만들어도 되는가 — 3분류

"사용자도 모르는 것을 만들지 않는다"를 실행 가능한 규칙으로 바꾼다.

| 분류 | 정의 | 조치 |
|---|---|---|
| **A** | 사용자가 요청한 것 | 만든다 |
| **B** | 요청하지 않았지만 **없으면 A가 성립하지 않는 것** | 만들되 **명세에 명시**하고 통보 |
| **C** | **있으면 좋은 것** | **만들지 않는다.** 백로그에 제안만 |

**C를 막는 규칙이 지금까지 없었다.** "이왕 하는 김에"가 누락의 반대편 실패다 —
사용자가 모르는 코드가 늘어나고, 검증 대상이 아니므로 아무도 확인하지 않는다.

### 11.5 명세에 "안 만드는 것"을 적는다

B와 C를 구분하려면 명세가 **범위의 경계**를 명시해야 한다.
`spec.md`에 고정 절을 둔다:

```markdown
## 이번에 만드는 것
· 물건 목록을 최신순으로 20개씩 보여준다
· 나눔완료된 물건은 흐리게 표시한다

## 이번에 안 만드는 것        ← 이쪽이 더 중요하다
· 검색 (F-006에서)
· 카테고리 필터 (백로그에 없음. 필요하시면 추가할게요)
· 무한 스크롤 (지금은 "더 보기" 버튼으로)
```

**"안 만드는 것"이 사용자에게 실제로 유용하다.** 기대와 결과의 차이를 미리 없앤다.
그리고 여기 적힌 것을 만들면 **범위 위반**으로 L3 대조에서 잡힌다.

### 11.6 기반 설비도 알린다

S4(골격)에서 에러 수집·문의 창구 같은 기반 설비를 넣기로 했다([JOURNEY §3.3](./JOURNEY.md)).
**사용자가 요청하지 않은 것이므로 반드시 알린다.**

> "만들 때 두 가지를 같이 넣어둘게요.
>  ① 오류가 나면 저한테 알려주는 장치
>  ② 나중에 쓰시는 분들이 문의할 수 있는 버튼
>  지금 넣어두면 공짜인데, 나중에 붙이려면 손이 많이 가서요."

**시드 데이터와 테스트 계정도 마찬가지다.** 만들었으면 말한다.

> **그리고 실 서비스 전환(`hasRealData`) 시 시드 데이터 정리를 반드시 묻는다.**
> 안 그러면 진짜 서비스에 "예시 물건 1"이 남아 있다.

### 11.7 병렬 구현은 위반이 아니다

`backend ∥ frontend`(④)는 **"기능 여러 개를 동시에"가 아니라 "기능 하나의 앞뒤면"**이다.
계약(§P3)이 경계를 잠그고 L3 대조가 누락을 잡으므로 유지한다.
**검증 단위는 여전히 기능 하나이고, 사용자가 보는 것도 하나다.**

### 11.8 강제 장치 정리

| 장치 | 방법 |
|---|---|
| AC 5개 / 작업 8개 / 30분 상한 | `devoffice gate spec`, `gate plan` |
| **미검증 기능이 있으면 다음 작업 차단** | `devoffice task start` 실패 + `Stop` 훅 |
| 명세에 "안 만드는 것" 절 필수 | `gate spec` |
| "안 만드는 것"을 만들면 범위 위반 | L3 증거 대조(§5.3) |
| 기반 설비·시드·테스트 계정 통보 | 프로듀서 스킬 규칙 |
| 실 전환 시 시드 정리 확인 | `hasRealData` 전환 절차 |
