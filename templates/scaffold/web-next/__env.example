# 여기 있는 값들은 "이런 게 필요하다"는 목록일 뿐입니다.
# 실제 값은 .env.local 에 넣습니다. 그 파일은 인터넷에 올라가지 않습니다.
#
# 새로운 값이 필요해지면 이 파일에도 이름을 추가해주세요.
# 그래야 나중에 다른 컴퓨터에서도 무엇이 필요한지 알 수 있습니다.

# Supabase — 데이터와 로그인
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
