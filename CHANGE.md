# 기능 수정 설계

> 지금까지의 설계는 **기능을 만드는 것**만 다뤘다.
> 그런데 실제 제품에서 **만드는 것보다 바꾸는 일이 더 많다.**
> 이 문서는 그 빈 자리를 메운다.

---

## 1. 왜 이게 어려운가

사용자는 이렇게 말한다.

> "댓글에 사진도 넣어주세요"
> "잔액 화면 좀 바꾸고 싶은데요"
> "이 기능 빼주세요"

**같은 문장이라도 지금 어느 상태인지에 따라 완전히 다른 일이 된다.**

| 상태 | "잔액 화면 바꿔주세요"의 실제 의미 |
|---|---|
| 목록에만 있음 | 제목 한 줄 고치면 끝 (10초) |
| 명세까지 됨 | 구체화를 다시 (15분) |
| 구현됨 | 코드·계약·데이터·검증까지 (1시간+, **되돌릴 수 없을 수도**) |

**사용자는 이 차이를 모른다.** 우리가 판정하고 알려줘야 한다.

---

## 2. 먼저 분류한다 — 수정인가 추가인가

비개발자는 "수정"과 "추가"를 구분하지 않는다. **우리가 나눠서 되말해준다.**

### 판정 기준

```
명세대로 만들었는데 그대로 안 되나?
  YES → 버그다. 기능 수정이 아니다 (§6)

기존 동작이 그대로 남는가?
  YES → 크기는?
          작다 (AC 1개 이하)  → 수정
          크다               → **새 기능**으로 쪼갠다
  NO  → 수정 + 영향 범위 검사 (§4)
```

### 예

| 사용자 말 | 기존 동작 | 판정 |
|---|---|---|
| "댓글에 사진도" | 그대로 남음 | **새 기능** — 목록이 10개→11개 |
| "500자를 1000자로" | 그대로 남음, 작음 | 수정 |
| "대댓글도 되게" | **구조가 바뀜** | 수정 + 데이터 영향 |
| "삭제 기능 빼주세요" | 없어짐 | 수정 |
| "댓글이 안 지워져요" | — | **버그** |

### 왜 추가는 새 기능으로 쪼개는가

1. **이미 확인받은 것을 다시 흔들지 않는다.** 수정으로 처리하면 검증 완료였던 기능이
   미검증으로 돌아가고 **진행 잠금에 걸린다**
2. **되돌리기가 쉽다.** 그 기능만 취소하면 된다
3. **진척도가 정직해진다.** 10개 중 5개 → 11개 중 5개

사용자에게는 이렇게 말한다.

```
그건 지금 있는 댓글은 그대로 두고 사진 기능을 얹는 거예요.
목록에 새 항목으로 넣을게요 — 10개에서 11개가 돼요.
지금 만들까요, 나중에 할까요?
```

---

## 3. 상태별 대응

```bash
devoffice feature impact <ID>
```

이 명령이 **지금 어느 상태이고 무엇이 영향받는지**를 알려준다. 추측하지 않는다.

### 상태 ① 목록에만 있음 (`pending`)

**가장 싸다. 그냥 고친다.**

| 바꾸는 것 | 대응 |
|---|---|
| 제목·설명 | 바로 고침 |
| 순서 | 바로 고침 |
| 삭제 | **의존성 확인 후** 삭제 |

**의존성만 본다.** F-003을 지웠는데 F-005가 그것에 기대고 있으면 F-005가 성립하지 않는다.

```
"전략 고르기"를 빼면 "전략 붙이기"도 못 만들어요.
 ① 둘 다 빼기   ② 그대로 두기   ③ "전략 붙이기"를 다르게 바꾸기
```

### 상태 ② 명세까지 됨 (`speced`)

**구체화를 다시 한다. 코드가 없으니 버릴 건 문서뿐이다.**

바뀌는 정도로 갈린다.

| 정도 | 대응 |
|---|---|
| AC 하나 고치기 | `spec.md` 부분 수정 → `gate spec` |
| **화면 구성이 바뀜** | **구체화부터 다시** (`devoffice-spec`) |
| 계약이 이미 있음 | 계약도 함께 고침. **다른 기능이 그 계약을 쓰면 §4** |

그림(Artifact)이 있으면 **같은 링크를 갱신한다.** 새로 만들면 어느 게 최신인지 헷갈린다.

### 상태 ③ 구현됨 (`done`) ★ 여기가 진짜 문제

**네 가지를 순서대로 확인한다.**

```
1. 데이터가 바뀌나?        ← 가장 위험. 실 데이터가 있으면 되돌릴 수 없다
2. 계약이 바뀌나?          ← 다른 기능이 깨질 수 있다
3. 다른 기능이 의존하나?
4. 검증 시나리오가 바뀌나?  ← 회귀 테스트도 같이 고쳐야 한다
```

**1번이 나머지 셋보다 무겁다.** §5 참조.

수정도 **기능 하나 만드는 것과 같은 절차**를 탄다 — 명세 → 계약 → 구현 → 검증.
다만 처음부터가 아니라 **바뀌는 부분만**.

> **수정한 기능은 다시 미검증 상태가 된다.** 사용자가 다시 확인해야 한다.
> "고쳐뒀어요"로 끝내면 안 고쳐진 채로 넘어간다.

---

## 4. 영향 범위 — 고치기 전에 보여준다

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  잔액 보기를 바꾸면
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  이것도 같이 바뀌어요
   · 수익률 비교 화면 — 잔액 계산을 같이 써요
   · 거래 기록 화면 — 같은 데이터를 봐요

  ⚠️ 저장된 데이터도 바뀌어요
     지금까지 기록된 잔액 500건에 새 항목이 생겨요.
     그 500건은 값이 비어 있게 됩니다.

  예상 시간 40분

👉 그래도 바꿀까요?
   ① 네      ② 다른 방법 알려주세요      ③ 그냥 둘게요
```

**"이것도 같이 바뀐다"를 안 보여주면 사용자는 나중에 놀란다.**
그리고 그때는 이미 다른 화면이 깨져 있다.

---

## 5. 데이터가 있을 때 — 되돌릴 수 없는 구간

`production.hasRealData` 가 `true` 면 완전히 다른 규칙이 적용된다.

### 반드시 순서대로

1. **백업 먼저.** 예외 없음
2. **기존 데이터를 어떻게 할지 사용자에게 묻는다** — 우리가 정할 수 없다
3. **되돌릴 수 없음을 명시**하고 확인받는다

### 기존 데이터 처리는 우리가 정하지 않는다

새 항목이 생기면 **이미 쌓인 것들은 그 값이 없다.** 어떻게 할지는 제품 판단이다.

```
지금까지 기록된 잔액이 500건 있어요.
새로 "실현손익" 항목이 생기는데, 그 500건에는 그 값이 없어요.

 ① 전부 0으로 채우기        간단해요. 다만 실제 값은 아니에요
 ② 비워두고 "-" 로 표시     정직해요. 화면에 빈칸이 보여요        ← 추천
 ③ 예전 것부터 다시 계산     정확하지만 오래 걸려요 (거래 기록이 있어야 해요)

 ⚠️ 이건 되돌릴 수 없어요. 고르시면 500건이 그대로 바뀝니다.
```

**②를 기본 추천으로 둔다.** 데이터를 지어내지 않는 쪽이 항상 안전하다.

### 항목을 빼는 것은 특히 위험하다

지우면 그 데이터는 사라진다. **지우지 말고 화면에서만 감추는 것**을 먼저 제안한다.

```
 ① 화면에서만 안 보이게 (데이터는 남아요)   ← 추천. 나중에 되돌릴 수 있어요
 ② 완전히 지우기 (되돌릴 수 없어요)
```

---

## 6. 버그는 기능 수정이 아니다

**"명세대로 만들었는데 그대로 안 된다"는 수정이 아니라 고침이다.**

- 명세를 바꾸지 않는다
- 새 기능으로 만들지 않는다
- 백로그에 추가하지 않는다
- **바로 고친다**

그리고 **회귀 테스트가 있었다면 이게 잡혔어야 한다**(v0.3). 못 잡았으면
고친 뒤에 그 시나리오를 검증 항목에 **추가**한다. 같은 버그가 두 번 나면 안 된다.

사용자에게는 이렇게 말한다.

```
그건 제가 잘못 만든 거예요. 바로 고칠게요. 10분쯤이요.
새로 만드는 게 아니라서 목록은 그대로예요.
```

**"제가 잘못 만들었다"고 먼저 말한다.** 비개발자는 자기가 잘못한 줄 알고 위축된다.

---

## 7. 되돌리기

기능 하나 = 커밋 하나이므로 **그 기능만 통째로 되돌릴 수 있다**(원칙 P7).

```bash
devoffice feature revert <ID>
```

**단, 그 뒤에 만든 것이 의존하면 못 되돌린다.** 먼저 `impact` 로 확인한다.
그리고 **실 데이터가 있으면 코드는 되돌아가도 데이터는 안 돌아온다** — 반드시 알린다.

```
"잔액 보기"를 만들기 전으로 되돌릴 수 있어요.

 ⚠️ 그 뒤에 만든 "수익률 비교"가 이걸 쓰고 있어요. 같이 되돌아가요.
 ⚠️ 저장된 데이터는 되돌아가지 않아요.

 ① 둘 다 되돌리기   ② 그냥 고치기   ③ 그대로 두기
```

---

## 8. 언제 수정을 말리는가

**말리지 않는다.** 사용자 제품이다.
다만 **더 싼 길이 있으면 먼저 보여준다.**

| 상황 | 먼저 제안할 것 |
|---|---|
| 데이터 항목 삭제 | 화면에서만 감추기 |
| 큰 구조 변경 | 새 기능으로 옆에 만들고 나중에 갈아타기 |
| 여러 기능이 얽힘 | 하나씩 나눠서 |

그리고 **비용을 정직하게 말한다.** "40분 걸려요"를 안 말하면 나중에 신뢰를 잃는다.

---

## 9. 무엇을 만들어야 하나

| # | 무엇 | 왜 |
|---|---|---|
| 1 | `backlog[].dependsOn` | 의존성 없이는 영향 범위를 알 수 없다 |
| 2 | `devoffice feature impact <ID>` | 상태 판정 + 영향 범위. **추측 금지** |
| 3 | `devoffice-change` 스킬 | 분류 → 판정 → 대응 |
| 4 | `devoffice feature reopen <ID>` | 구현된 기능을 수정하러 다시 여는 길 |
| 5 | `devoffice feature revert <ID>` | v0.3 (git 태그 필요) |

---

## 10. 한 줄 요약

> **수정은 "무엇을 바꾸나"가 아니라 "지금 어느 상태인가"의 문제다.**
> 상태를 먼저 판정하고, 영향 범위를 보여주고, 데이터가 걸리면 반드시 묻는다.
